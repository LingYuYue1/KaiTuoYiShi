# 记忆系统优化 · 阶段1制作报告

> 本报告供牢凌审查阶段1的全部改动。每项改动包含：改了什么、为什么改、怎么实现、玩家感受、验证状态。
> 审查完成后可合并到 main 分支。

---

## 一、阶段1目标回顾

**核心目标**：补齐墨色江湖记忆系统的5个关键机制 + 手机记忆双轨 + 约定系统接入，解决 80-90% 的失忆问题。

**完成度**：100%（代码层面全部就位，已通过类型检查+构建+回归测试）

---

## 二、改动清单（按功能模块）

### 模块1 · 墨色5机制（核心失忆修复）

#### 1.1 原文每回合入库（机制①）

- **改了什么文件**：`services/yitingArchive.ts`、`hooks/useGame/sendWorkflow.ts`
- **为什么改**：以前每回合剧情原文用完就丢，AI 只能靠压缩摘要回忆，细节丢失严重
- **怎么实现**：每回合结束后，原文自动存进忆庭档案库，永久保留
- **玩家感受**：NPC 不会忘记几回合前发生的事的细节（礼物、约定、对话）

#### 1.2 长期记忆全量注入 + 压缩机制对齐墨色（机制②）

- **改了什么文件**：`components/features/Memory/MemorySummaryFlowModal.tsx`（新建）、`hooks/useGame/memoryUtils.ts`、`hooks/useGameState.ts`、`App.tsx`
- **为什么改**：以前压缩是同步静默执行的，玩家看不到过程也无法干预
- **怎么实现**：新增三阶段压缩弹窗（提醒→处理→审核），玩家可查看/编辑/确认
- **玩家感受**：压缩不再是黑箱，玩家能掌控记忆质量

#### 1.3 取消忆庭互斥（机制③）

- **改了什么文件**：`hooks/useGame/sendWorkflow.ts`、`hooks/useGame/systemPromptBuilder.ts`
- **为什么改**：以前忆庭召回和普通记忆注入是二选一，开了忆庭就关掉普通记忆
- **怎么实现**：忆庭召回和普通记忆注入并存，AI 同时看到摘要层和原文层
- **玩家感受**：NPC 记忆更完整，既记得大概剧情也记得关键细节

#### 1.4 玩家承诺结构化载体（机制④·约定系统）

- **改了什么文件**：`models/npc.ts`、`hooks/useGame/systemPromptBuilder.ts`、`services/ai/variableModel.ts`、`utils/variableFacts.ts`
- **为什么改**：以前玩家跟 NPC 的约定混在普通记忆里，压缩后容易丢，NPC 经常失约
- **怎么实现**：约定作为独立结构挂 NPC 下，三环联动：
  - 写入环：variableModel 每回合从对话+通讯回忆提取约定
  - 注入环：未完成的约定永久注入 NPC 档案
  - 清理环：约定完成后自动标记，不再注入
- **玩家感受**：NPC 会主动提起约定（"我们不是说好今天去逛街吗"），不会失约

#### 1.5 原文永久保留（机制⑤）

- **改了什么文件**：无需改代码，忆庭系统设计上独立于压缩链
- **为什么改**：以前原文会被压缩链"吃掉"，压完就没了
- **怎么实现**：忆庭档案库原文层只读，压缩链只动摘要层
- **玩家感受**：任何时候翻忆庭都能看到完整原文

#### 1.6 NPC 记忆压缩对齐墨色（机制⑥）

- **改了什么文件**：`hooks/useGame/memoryUtils.ts`（新增 `compressNpcMemoryLedgerAsync`）、`services/memoryCompression.ts`、`models/memory.ts`、`models/settings.ts`
- **为什么改**：以前 NPC 同行记忆没有独立压缩，攒多了就乱塞
- **怎么实现**：
  - NPC 记忆独立压缩，阈值 20 条（对齐墨色）
  - 压缩时保留最近 5 条不压缩
  - 压缩失败兜底（前3条去重各截28字+省略号）
  - 提示词明确保留关系变化、约定、情绪余温等
- **玩家感受**：NPC 对玩家的态度变化、关系进展不会因记忆多而丢失

---

### 模块2 · 审查补齐（5项细节优化）

#### 2.1 中期记忆改全量注入 + 短期窗口对齐30

- **改了什么文件**：`hooks/useGame/memoryUtils.ts`、`hooks/useGame/systemPromptBuilder.ts`
- **为什么改**：以前中期记忆被窗口截掉，中短期事实丢失
- **怎么实现**：中期记忆全量注入，短期注入窗口对齐 30 条
- **玩家感受**：中期事件不会突然被遗忘

#### 2.2 recall 分档注入

- **改了什么文件**：`services/yitingRetrieval.ts`
- **为什么改**：以前 recall 命中时细节有损
- **怎么实现**：召回 ≤6 条全原文，>6 条 Top5 原文 + 其余摘要
- **玩家感受**：recall 命中时关键细节保留更完整

#### 2.3 压缩链噪声过滤

- **改了什么文件**：`hooks/useGame/memoryUtils.ts`、`utils/variableFacts.ts`
- **为什么改**：V3 进度元数据污染记忆链（storyProgressMemoryLine 等）
- **怎么实现**：三层过滤（提示词约束 + 输出清理 + 写入时 pattern 过滤）
- **玩家感受**：记忆链不再混入"动态世界""行动选项"等系统广播

#### 2.4 忆庭归档噪声过滤

- **改了什么文件**：`services/yitingArchive.ts`、`hooks/useGame/memoryUtils.ts`
- **为什么改**：V3 进度元数据污染忆庭归档
- **怎么实现**：合并两套噪声模式到统一的 `MEMORY_SYSTEM_NOISE_PATTERNS`（16个模式），`yitingArchive.ts` 复用 `isMemorySystemNoise`
- **玩家感受**：忆庭档案不再混入系统广播，只有真实事件

#### 2.5 recall 候选阶段给前 N 条原文

- **改了什么文件**：`services/yitingRetrieval.ts`
- **为什么改**：recall 检索质量受 API 重排材料不全影响
- **怎么实现**：候选阶段就给前 N 条原文，供 API 重排
- **玩家感受**：AI 重排时材料更全，召回更准

---

### 模块3 · 手机记忆双轨（方案E）

#### 3.1 忆庭通讯记忆 + NPC同行记忆双写

- **改了什么文件**：`components/features/Phone/PhoneModal.tsx`、`services/yitingRetrieval.ts`、`models/yiting.ts`、`components/features/GameSystems/YitingPanel.tsx`、`hooks/useGame/systemPromptBuilder.ts`
- **为什么改**：以前手机聊天写回 NPC 记忆不稳定（依赖 AI 输出），导致正文里 NPC 跟玩家像陌生人
- **怎么实现**：
  - 忆庭条目新增 `分类` 字段（'正文' | '通讯'），旧条目默认'正文'
  - 手机压缩摘要代码强制双写到：① 忆庭（分类='通讯'，API精炼）② NPC同行记忆[来源='手机'，打包1条]
  - 手机压缩阈值调高：私聊 20 条 / 群聊 30 条
  - 手机压缩完成时立即双写（不等下一回合）
  - NPC同行记忆中手机对话打包成单条，用【通讯记录】标记
- **玩家感受**：手机里聊得熟了，正文里 NPC 也表现出亲近感

#### 3.2 全知性防护（防止 NPC 全知）

- **改了什么文件**：`hooks/useGame/systemPromptBuilder.ts`
- **为什么改**：防止 NPC 莫名其妙知道玩家手机里的私密对话
- **怎么实现**：
  - 通讯记忆注入时标记【通讯回忆】(对方:XXX)
  - 只有通讯对方 NPC 能看到对应记忆
  - 三层防护：上下文隔离 + 来源标记 + 提示词约束 + 通讯对方在场判断（代码层）
- **玩家感受**：其他 NPC 不会突然知道你手机里聊了什么

#### 3.3 手机压缩双写原子性修复

- **改了什么文件**：`components/features/Phone/PhoneModal.tsx`
- **为什么改**：双写没有 try/catch，第二步失败会导致数据不一致
- **怎么实现**：
  - 步骤1（忆庭侧）和步骤2（NPC同行记忆侧）各自独立 try/catch
  - 失败记录到 `phoneWriteFailures` 重试队列（含 summary/contact/failedStep/error/timestamp）
  - 新增 `retryPhoneWriteFailure(index)` 手动重试函数
  - 双写都失败时提示玩家
- **玩家感受**：手机记忆不会因单步失败而丢失，可手动重试

#### 3.4 忆庭 UI 通讯标签页

- **改了什么文件**：`components/features/GameSystems/YitingPanel.tsx`
- **为什么改**：玩家需要单独查看手机记忆
- **怎么实现**：忆庭面板新增"通讯回忆"标签页，数据层分类 + UI 展示同步
- **玩家感受**：可以单独翻看手机聊天记忆，和正文记忆分开管理

#### 3.5 手机忆庭精炼提示词

- **改了什么文件**：`prompts/` 相关提示词文件
- **为什么改**：手机通讯内容特性与正文不同，需要专门提示词
- **怎么实现**：针对通讯内容优化提示词，明确时间锚点、参与者、关键信息提取
- **玩家感受**：手机记忆精炼质量更高，保留关键互动信息

#### 3.6 多会话独立压缩 + API失败兜底

- **改了什么文件**：`components/features/Phone/PhoneModal.tsx`、`services/yitingArchive.ts`
- **为什么改**：多会话并发时不独立压缩会混淆；API 失败时记忆会丢
- **怎么实现**：
  - 多会话独立压缩、独立进忆庭
  - API 失败兜底复用 `createFallbackArchiveEntry`，使用本地压缩结果
- **玩家感受**：多会话不会互相干扰，API 挂了也不丢记忆

---

### 模块4 · 约定系统接入伙伴面板（展示环）

#### 4.1 扩展 NPC 记忆账本视图

- **改了什么文件**：`models/npc.ts`
- **为什么改**：约定数据需要暴露给 UI 层展示
- **怎么实现**：
  - `NPC记忆账本视图` 新增 `约定: 约定结构[]` 字段（含全部状态）
  - `buildNpcMemoryLedgerView` 填充 `record.约定 ?? []`
  - `有账本字段` 检查项加入 `record.约定?.length`

#### 4.2 软上限自动清理

- **改了什么文件**：`utils/variableFacts.ts`
- **为什么改**：约定数组只增不减，长期使用后会膨胀
- **怎么实现**：
  - `agreement_status` 事实处理分支追加清理逻辑
  - 已完结约定（状态≠等待中）按回合降序保留最近 20 条，超出部分删除
  - 等待中约定永不清理
  - 触发清理时 push warning 记录清理条数
  - 常量 `COMPLETED_AGREEMENT_KEEP = 20`，对齐 NPC 同行记忆压缩阈值

#### 4.3 伙伴面板新增约定标签页

- **改了什么文件**：`components/features/GameSystems/CompanionPanel.tsx`
- **为什么改**：约定三环联动只实现了写入/注入/清理，缺"展示环"让玩家可见可编辑
- **怎么实现**：
  - `DetailTab` 类型新增 `'agreement'` 选项
  - 新增 `AgreementPanel` 子组件：
    - 状态筛选（全部/等待中/已完结）
    - 统计计数（总数/等待中/已完结）
    - 状态徽章（黄=等待中/绿=已履行/红=已违约/灰=已作废）
    - 手动编辑状态（下拉选择 等待中→已履行/已违约/已作废）
    - 直接删除
  - 通过 `onUpdateNpc: (patch: Partial<NPC记录>) => void` 双向绑定到 NPC 记录存储
  - 空状态友好提示"暂无约定记录"
- **玩家感受**：约定从"AI内部数据"升级为"玩家可管理资产"，可在伙伴面板查看/编辑/删除

#### 4.4 约定三环联动升级为四环

- 写入环（variableModel 提取）
- 注入环（等待中常驻 NPC 账本）
- 清理环（状态变更 + 软上限20条自动清理）
- **展示环**（伙伴面板可见可编辑，本次新增）

---

### 模块5 · 手动压缩入口

#### 5.1 主链压缩手动入口

- **改了什么文件**：`components/features/GameSystems/MemoryPanel.tsx`、`App.tsx`
- **为什么改**：以前只能等自动触发，玩家想主动压缩做不到
- **怎么实现**：
  - 记忆面板新增"立即压缩记忆"按钮
  - 实时显示待压缩条数（即X/短Y/中Z）
  - 无需压缩时灰显"暂无需压缩"
  - 点击触发三阶段弹窗（复用已有的 remind→processing→review）
- **玩家感受**：玩家可主动控制压缩节奏，剧情节点时主动整理

---

### 模块6 · 阈值对齐 + 测试基线

#### 6.1 阈值对齐墨色

- **改了什么文件**：`models/settings.ts`
- **怎么实现**：
  - 即时→短期：10 条（对齐墨色）
  - 短期→中期：30 条（对齐墨色，原25）
  - 中期→长期：50 条（对齐墨色）
  - NPC 记忆：20 条（对齐墨色）
  - 旧存档 25/20/10 自动迁移到 15/15/15（向前兼容）

#### 6.2 测试基线对齐（7个回归测试更新）

- **改了什么文件**：
  - `scripts/memory-compression-threshold-regression.mjs`
  - `scripts/npc-memory-continuity-regression.mjs`
  - `scripts/npc-profile-ledger-regression.mjs`
  - `scripts/yiting-archive-regression.mjs`
  - `scripts/phone-main-continuity-regression.mjs`
  - `scripts/background-task-mode-regression.mjs`
  - `scripts/independent-system-prompt-scope-regression.mjs`
- **为什么改**：阶段1改变了阈值/注入策略/函数名，旧测试断言不再匹配
- **怎么实现**：所有记忆相关测试断言更新对齐新机制

---

## 三、验证状态

| 验证项 | 结果 | 说明 |
|---|---|---|
| TypeScript 类型检查 | ✅ 通过 | exit 0 |
| 全量构建（npm run build） | ✅ 通过 | 3分12秒，仅 chunk size 警告 |
| 记忆相关回归测试 | ✅ 全部通过 | 7个测试全部对齐 |
| 阶段1代码就位 | ✅ 100% | 6模块全部完成 |

**test:all 总体**：199/228 通过。剩余 29 个失败与本次改动无关（智库 matcher / 剧情编制V3基线 / 云备份 / 环境问题等预先存在）。

---

## 四、改动文件清单（汇总）

### 代码文件

| 文件 | 改动类型 | 说明 |
|---|---|---|
| `models/npc.ts` | 修改 | 新增 `约定结构`、`约定状态` 类型；`NPC记录` 加 `约定` 字段；`NPC记忆账本视图` 加 `约定` 字段 |
| `models/yiting.ts` | 修改 | 忆庭条目加 `分类` 字段（'正文' \| '通讯'） |
| `models/memory.ts` | 修改 | 加 'npc' 压缩层级 |
| `models/settings.ts` | 修改 | 默认阈值改为 10/30/50/20；NPC 压缩提示词优化 |
| `services/yitingArchive.ts` | 修改 | 原文入库；噪声过滤复用 `isMemorySystemNoise`；fallback 加噪声过滤 |
| `services/yitingRetrieval.ts` | 修改 | recall 分档注入；候选阶段给前N条原文；忆庭召回加分类过滤 |
| `services/memoryCompression.ts` | 修改 | 加 'npc' 压缩类型 |
| `services/ai/variableModel.ts` | 修改 | 扩展 recallContext，从正文+通讯回忆提取约定 |
| `hooks/useGame/sendWorkflow.ts` | 修改 | 原文每回合入库；取消忆庭互斥 |
| `hooks/useGame/systemPromptBuilder.ts` | 修改 | 取消忆庭互斥；NPC档案注入分离手机/正文记忆；全知性防护 |
| `hooks/useGame/memoryUtils.ts` | 修改 | 新增 `compressNpcMemoryLedgerAsync`；中期全量注入；压缩链噪声过滤；合并 `MEMORY_SYSTEM_NOISE_PATTERNS`（16个模式） |
| `hooks/useGameState.ts` | 修改 | 三阶段压缩弹窗状态管理 |
| `utils/variableFacts.ts` | 修改 | 约定写入；约定状态变更；软上限自动清理（20条）；写入时 pattern 过滤 |
| `components/features/Memory/MemorySummaryFlowModal.tsx` | 新建 | 三阶段压缩弹窗（remind→processing→review） |
| `components/features/GameSystems/MemoryPanel.tsx` | 修改 | 手动压缩入口按钮 |
| `components/features/GameSystems/YitingPanel.tsx` | 修改 | 新增"通讯回忆"标签页 |
| `components/features/GameSystems/CompanionPanel.tsx` | 修改 | 新增 `AgreementPanel` 子组件 + 约定标签页 |
| `components/features/Phone/PhoneModal.tsx` | 修改 | 手机压缩双写；双写原子性修复（try/catch+重试队列） |
| `App.tsx` | 修改 | 挂载三阶段弹窗；`handleTriggerManualCompress` 回调 |

### 测试文件

| 文件 | 改动类型 |
|---|---|
| `scripts/memory-compression-threshold-regression.mjs` | 修改 |
| `scripts/npc-memory-continuity-regression.mjs` | 修改 |
| `scripts/npc-profile-ledger-regression.mjs` | 修改 |
| `scripts/yiting-archive-regression.mjs` | 修改 |
| `scripts/phone-main-continuity-regression.mjs` | 修改 |
| `scripts/background-task-mode-regression.mjs` | 修改 |
| `scripts/independent-system-prompt-scope-regression.mjs` | 修改 |

### 提示词文件

| 文件 | 改动类型 |
|---|---|
| 手机忆庭精炼提示词 | 新增 |
| NPC 记忆压缩提示词 | 优化 |
| 约定提取 JSON schema | 新增 |

---

## 五、已知限制与设计取舍

### 5.1 已知限制

| 限制 | 说明 | 影响 |
|---|---|---|
| 约定来源硬编码 | `variableFacts.ts` 里约定来源固定为 `'正文'`，未根据 recallContext 判断 | 低。能记录即可，来源标记不影响功能 |
| 剩余29个回归测试失败 | 与本次改动无关（智库/剧情V3/云备份/环境问题） | 不影响阶段1功能 |
| 忆庭 AI 重排不输出分数 | 玩家看不到召回的相关度数值 | 低。可通过小改让 AI 输出分数 |

### 5.2 设计取舍

| 取舍点 | 决策 | 理由 |
|---|---|---|
| 向量检索（阶段2） | 搁置 | 阶段1的 AI 重排比向量 cosine 语义理解更强；向量边际收益有限 |
| 保留楼层机制（阶段3） | 不做 | 本项目的压缩链 + 忆庭原文层已等效实现，且更安全 |
| Active Tool（阶段3） | 不做 | 本项目的 AI 重排已经是 AI 主动召回，只是实现路径不同 |
| 提示词整体重组 | 留到后期 | 记忆机制稳定后再重组，避免反复改 |

---

## 六、阶段1 vs 墨色江湖对照

| 墨色机制 | 阶段1实现 | 状态 |
|---|---|---|
| 原文每回合入库 | 忆庭原文层每回合自动存 | ✅ |
| 全量注入 + 压缩对齐 | 三阶段弹窗 + 阈值 10/30/50 | ✅ |
| 取消忆庭互斥 | recall 标签并存 | ✅ |
| 约定结构化载体 | 约定系统三环联动 + 展示环 | ✅ |
| 原文永久保留 | 忆庭原文层只读 | ✅ |
| NPC压缩对齐墨色 | 调AI + 三阶段弹窗 + 阈值20 | ✅ |

---

## 七、审查建议

建议牢凌从以下几个角度审查：

1. **功能完整性**：6个模块是否覆盖了阶段1的所有目标
2. **数据一致性**：手机双写、约定清理、压缩链是否会破坏存档
3. **玩家体验**：三阶段弹窗、手动压缩、约定面板的交互是否顺畅
4. **性能影响**：每回合原文入库、AI 重排、压缩弹窗对响应速度的影响
5. **向前兼容**：旧存档迁移逻辑是否覆盖所有场景

审查完成后，如无问题可合并到 main 分支。

---

## 八、下一步方向（阶段1之后）

阶段2/3（向量检索）已搁置，后续可考虑的方向：

1. **提示词整体重组**（路线图后续计划）
2. **忆庭关键词检索升级**（同义词扩展、AI 改写查询）
3. **AI 重排输出分数**（补齐可视化调试）
4. **剩余29个回归测试收尾**
5. **实际游玩验证**（玩几回合感受记忆效果，发现问题再调整）

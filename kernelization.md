# 理想内核设计蓝图（第六版）

**文档定位**：本文档描述目标状态的设计。执行边界以 `AGENTS.md` 为准。当前事实与差距以 `ideal_design.md` 为准。本文档全部内容属于 `[TARGET]`，不描述当前已经具备的行为。

**远期方向**：本文档的实现按与未来 Rust 核心同构的形态设计。能力句柄对应 Rust 的 Trait，命令总线对应 IPC invoke，投影对应 Selector，恢复对应 Savepoint。当前实现语言保持 TypeScript。Rust、Tauri、WASM、OPFS 属于远期迁移形态，见第 18 节。当前的增量重构路线图不受本文档影响。

**文档结构**：本文档分两部。第一部是内核设计层，定义内核的不变量、执行机制、槽位契约与服务接口。第二部是子系统与插件实现层，定义在内核机制下运行的各子系统、插件逻辑与数据结构归属。领域术语以 `CONTEXT.md` 为准。

**本版变更**：本版按 2026-08-11 的设计讨论整体重构。主要变化如下：

1. 文档划分为两部：内核设计层与子系统/插件实现层。
2. 新增通道哲学：槽位是时间刻度，钩子是插件劳动，通知是事实广播，并附流式场景推演。
3. fields 声明扩展为两个维度：可见性（默认 private）与生命周期（默认 permanent，可标记 ephemeral）。commitLeaf 按 ephemeral 标记剥离，剥离不损坏内存中的 ephemeral 值。
4. 中断恢复的重跑粒度定为六槽位整体重跑。纯计算槽位自动重跑，含外部费用的槽位必须经用户确认。运行时失败与中断恢复共用同一入口。
5. queueTasks 不再是内核 ephemeral 字段，划归正文生成插件自有的 permanent 字段。内核不管理队列，插件订阅 turnCreated 后自行投递下一回合。
6. 迟到的非阻塞结果内核不介入，属插件内部事务。deviceSetting 升级为 deviceStorage，作为第五项服务句柄。
7. 会话标识明确为内存临时 ID：经存档管理器切换分支时重新生成，常规回档不变，不落存档。
8. 自定义槽位注册时必须声明错误级别，默认后台。
9. 手动整理范围定死为三项封闭集。
10. 领域术语表独立为 `CONTEXT.md`。
11. 槽位内的钩子组合采用洋葱中间件模型，链内可变、链外不可变，终局必须显式。
12. 插件定义为 umbrella term：一个业务插件由内核模块与可选的界面模块组成，模块间只经投影与命令通信，禁止任何后门。
13. 界面层引入插槽体系：界面框架声明插槽，界面模块绑定投影与命令。
14. 流式文本必经对话管理插件过滤后进入引擎私有投影通道；chatHistory 只存过滤后文本。AI 响应不做指纹缓存。
15. 公共目录的地位定稿：领域状态的唯一真相是活跃叶子，公共目录是其读视图；服务句柄管状态之外的世界，两者不竞争真相地位。

---

# 第一部 内核设计层

## 1. 设计哲学

以下五条原则是系统的最高裁决依据。任何实现不得走违反这些原则的捷径。

1. **命令与查询分离，界面极度无脑化**。界面不知道底层存储结构，不订阅具体变量名，不自己修改状态。界面只是内核状态在某一刻的只读投影：`View = f(StateSlice)`。
2. **插件是客，内核是主**。插件不能直接使用系统的基础设施。控制权完全倒置。插件没有自己的生命周期，没有常驻的私有订阅。插件只在回合引擎指定的槽位中被唤醒，执行完毕立即交出控制权。
3. **回合即事务**。执行管线无论多长，本质上是一次状态跃迁。系统设立绝对的安全检查点。事务只有两个结局：没抵达检查点则回滚丢弃，抵达了检查点则下游逻辑可重试。系统不做"从第几步中断处继续"的脆弱设计。
4. **数据树指针化与结构共享**。系统拒绝深度拷贝与直接覆写。一切状态变动都是在不可变的状态树上长出新叶子并移动头指针。这为低开销的状态比对和时间旅行提供物理基础。
5. **跨越环境边界**。核心业务逻辑不与宿主环境耦合。一切副作用操作抽象为能力接口。切换实现即可切换运行环境。

## 2. 分层结构

系统包含两层，依赖方向单向：

1. **界面层**包含界面框架、界面组件和场景编排。组件是无逻辑的。界面框架声明插槽区域。
2. **内核层**包含存储引擎、全局指针、树操作、回合引擎、插件宿主、状态归约器和全部插件的内核模块。

界面层依赖内核层。反向依赖被禁止。插件在编译期静态注册，系统不提供运行时扩展加载。

**插件是 umbrella term**。一个业务插件由两部分组成：

- **内核模块**（必须）：fields、hooks、events、projections 四组声明，运行在内核层。内核模块对界面布局零感知，只通过投影发布数据。
- **界面模块**（可选）：运行在界面层，把内核模块的投影绑定到界面框架的插槽，把按钮绑定到命令名。界面模块的权限上限等于界面层的三种动作（调命令、订阅投影、接收推送），碰不到公共目录、writeLeaf 和服务句柄。

同一插件的两个模块之间只允许经投影、命令、推送通信。禁止共享内存对象、直接 import、私有事件等任何后门。该纪律同时是为远期 Rust 化做准备：内核模块迁往 Rust 后，跨模块通信必然跨越语言边界，任何依赖同进程假设的耦合都会在迁移时爆炸。

### 2.1 界面层的无状态纪律

界面层保持无状态。组件只允许持有白名单内的本地临时状态：

1. 未提交的草稿，例如输入框文本、表单编辑中的值。
2. 纯视觉瞬态，例如悬停、焦点、动画进行中、滚动位置。
3. 手势与交互进行中，例如拖拽中。
4. 一次性提示，例如 toast。

白名单之外的界面状态被禁止。面板展开折叠这类偏好属于设备存储（见第 11 节），走持久化，不算本地临时状态。导航状态 view 是界面层自有的瞬态，不进内核，不进叶子。

## 3. 通道哲学：槽位、钩子、通知

三者不是平级概念。**槽位是位置，钩子是附着物，通知是广播**。

- **槽位（slot）是内核的时间轴刻度**。它是回合引擎状态机上的点，内核独有。插件不能发明时间，只能申请在某个刻度被叫醒；自定义槽位也必须挂载到六刻度之下。
- **钩子（hook）是插件在刻度上的劳动**。方向是内核到插件：内核调用，插件单次返回增量，控制权即刻归还。钩子同步、有返回值、参与回合事务。
- **通知（notify）是事实广播**。方向是内核或插件到订阅方：声明"某事已经发生"。通知不带数据、不要返回值、异步执行。通知不参与回合事务，订阅方被唤醒后自己回公共目录重读。

判定标准：**需要返回值的不是通知；产生状态归属的不是服务；需要内核调度时机的不是自由回调**。三条通道加上读（公共目录）、写（writeLeaf）、界面动作（命令），构成全部耦合通道，系统不存在第六条。

### 3.1 场景推演：调用 LLM 并流式更新界面

以"用户发送消息，正文生成插件调用 LLM API，文本流式更新到界面"推演三者的职责与数据流向：

1. **入口是命令，不是槽位**。界面调用"投递输入"命令，命令把输入放进 input 槽位，引擎启动回合。界面不编排任何后续步骤。
2. **槽位推进，钩子劳动**。preModel 一刻，正文生成插件的钩子被唤醒，从公共目录实时读取所需字段，构建提示词，作为不可变增量返回，引擎合并进回合上下文。
3. **modelCall 扩展点发起流式请求**。AI 策略插件执行实际请求。流式文本先经过对话管理插件的流式过滤（剥离 think 标签、敏感词替换等），再进入引擎的私有投影通道。流式文本是瞬态派生数据：没有状态归属、不进叶子、不经公共目录、不经事件。界面直接订阅引擎渲染气泡。
4. **落地只有一次写入**。postLanding 一刻，对话管理插件的解析结果（过滤后的完整正文）经 writeLeaf 写入 chatHistory，回合计数递增。界面看到的与叶子存下的是同一份过滤后文本。这是回合唯一的检查点。chatHistory 字段变脏触发精确失效，正式投影重算并推送，界面从流式投影切换到正式投影。流式通道随即作废。
5. **通知只出现在异步边界**。本场景的正常路径不需要任何通知。turnCreated 在 commitBoundary 之后才触发，用于唤醒回合外的订阅方（例如正文生成插件投递下一条排队消息）。

该场景验证三元分工：流式更新是投影（只读派生数据），不是通知（没有需要异步订阅方反应的事实），更不是钩子（方向相反，是内核到界面）。界面的两份数据源（流式投影与正式投影）都是无状态订阅，切换由字段失效驱动。

## 4. 界面层与内核层的交互

界面层对内核层只有三种动作：**调用命令、订阅投影、接收推送**。

### 4.1 命令总线

- 内核注册内建命令：投递输入、writeLeaf、forkLeaf、deleteNode、hydrate、手动整理。
- 插件注册自己的用例命令，例如相册的生成图像。界面按命令名直接调用。
- 命令注册在编译期静态完成。
- 命令是调用壳，不是新的写入通道。命令内部的写入只能经 writeLeaf 和归约器。命令不允许绕开字段校验。
- 命令只触发动作。命令的执行结果一律经投影回流到界面，命令不直接返回业务数据。

### 4.2 投影订阅与精确失效

- 插件在注册投影函数时，必须显式声明它依赖的 public 字段。
- 全部写入都经过 writeLeaf 这一个漏斗。内核在应用写入的那一刻知道哪个字段变了。
- 内核根据依赖声明维护字段到投影的反向索引。字段变脏时，内核只重算依赖该字段的投影，并把重算后的界面切片推送给订阅者。
- 界面收到推送后更新对应组件。界面不缓存投影数据本体。
- 界面框架声明插槽区域（侧栏面板、消息操作栏、输入工具栏、角色卡页签等）。插槽清单是界面层的实现细节，不进入本文档。插件的界面模块把投影绑定到插槽、把按钮绑定到命令名，由界面框架统一渲染。内核侧不知道挂载关系的存在。

### 4.3 手动编辑的写回时机

手动编辑只在用户点击保存时触发一次 writeLeaf。系统不做失焦自动写入，不做逐键写入。未保存的草稿是白名单内的本地状态。编辑期间管线读到的是叶子里的旧值。

## 5. 内核的对外接口

### 5.1 五个树操作

| 操作 | 语义 |
| --- | --- |
| writeLeaf | 提交突变函数。内核在应用的那一刻对当前值求值。用户面板的手动编辑与管线写入走同一通道，内核按修改时序把两者串行化。任何一方都不整体覆盖字段。钩子执行过程中允许多次 writeLeaf。 |
| commitLeaf | 封存当前叶子为检查点，创建新叶子，移动指针。这是唯一的永久保存入口，在单个事务内完成。它只在回合完整结束时由引擎在 commitBoundary 调用。回合未完成的叶子不封存，也不会成为父节点。封版时按 ephemeral 标记剥离仅限活跃叶子的字段。 |
| forkLeaf | 从任意检查点分叉新叶子并移动指针。读取存档、回档、重新生成都使用该操作。重新生成不经过 input 槽位。 |
| deleteNode | 级联修剪子树。指针落在被删集合时，重定向到最近存活的祖先节点。删除时对被删子树的每个节点解析资产引用并逐个递减引用计数。 |
| hydrate | 把节点转换为完整的领域状态。这是唯一的水合入口，系统只在这里执行归一化。启动、继续、中断恢复都使用该操作。 |

### 5.2 全局指针

系统只保留一个 newest 指针。它是一条单记录，包含 rootId 与 nodeId，指向当前活跃叶子。它的职责是让系统启动时快速定位工作区。

### 5.3 资产服务与引用计数

大体积二进制数据维持指针化存储。存档只持有指针引用。写入时分离二进制数据，读取时填充。

内核向插件暴露资产服务句柄：写入资产、读取资产、删除资产。插件只能通过句柄操作资产。

引用计数规则对称，共三个落点：

1. 任何节点诞生时，它携带的全部资产引用计数加一。writeAsset 创建资产时归属当前叶子，计数加一。commitLeaf 创建新叶子时，新叶子继承父节点字段，继承的引用计数加一。
2. 叶子封存为检查点是同一节点的身份转变。引用关系不变，计数不动。
3. deleteNode 删除节点时，对被删子树的每个节点解析引用并逐个减一。计数归零的资产由系统回收。

### 5.4 手动整理入口

系统提供一个手动触发的一键整理功能。整理范围是封闭的三项：

1. 把没有父节点的散落存档点整理为根节点。
2. 回收孤儿资产。
3. 校正资产引用计数。

整理功能复用资产服务句柄实现。超出此范围的整理能力需要修改本文档。

### 5.5 存储内部细节

增量编码与摘要目录是存储引擎的内部细节。上层模块不感知。

## 6. 回合引擎与六槽位

### 6.1 所有权

回合引擎是内核的职能。界面层向 input 槽位投递输入，这是界面层影响回合的唯一方式。引擎看到输入后启动回合，按状态机逐刻推进，自己调用 commitLeaf 封版。启动时机、执行顺序、封版时机全部在内核手里。

### 6.2 六槽位

回合从一次 turnCreated 到下一次 turnCreated，经过六刻：

1. **input**：引擎的唯一入口。产出契约只有一条：槽位结束时固定输入变量必须有合法值，否则判回合失败；或者钩子显式返回终局增量提前结束回合，见 6.3。输入的加工是插件的内部事务。
2. **preModel**：模型前置。召回类任务在此并行。提示词构建是正文生成插件的内部实现，注册在此槽位。
3. **modelCall 扩展点**：preModel 与 postLanding 之间的策略注入点。AI 插件以策略形式注册，负责实际请求、重试与解析。引擎只做流式投影与中断控制。流式文本先经对话管理插件的流式过滤，再走引擎的私有投影通道推给界面，不经过事件和公共目录。流式处理者限定为对话管理插件一家，不开放为插件间自由挂载的过滤器链。
4. **postLanding**：正文落地。过滤后的正文写入 chatHistory，回合计数递增。这是回合的唯一检查点（Savepoint）。chatHistory 只存过滤后文本，原始模型输出是协议细节，不入叶子。
5. **settlement**：结算。归约相位、非玩家角色结算、剧情编织结算强制串行。记忆结算、世界与旅人结算与之并行。
6. **backgroundJobs**：后台任务。插件注册钩子时声明 blocking 级别。引擎在 commitBoundary 之前只等待 blocking 钩子。非 blocking 任务携带会话身份与目标叶子标识。任务迟到时的处置（校验、保存、通知、应用或丢弃）全部是插件内部事务，内核不介入，见 14.3。

commitBoundary 见 6.4。

### 6.3 槽位的统一执行契约

六个槽位执行同一套契约。**槽位内的钩子以洋葱中间件模型组合**，引擎按编译期注册表顺序组装中间件链：

- 中间件接收 `(draft, next)`。draft 是本槽位的回合上下文草稿，链内可变；链结束后引擎统一校验并合并进回合上下文，链外不可变。回合上下文是引擎持有的内存对象，不落叶子。需要渐进产出的数据走 writeLeaf 写自有字段。
- 中间件不调用 `next()` 的效果只限于跳过本槽位的剩余钩子。**回合级终局必须显式**：钩子返回终局增量（如 `turnOutcome: 'consumed'`），引擎据此跳过后续槽位直接进入 commitBoundary 或判回合失败。禁止以"永不调用 next()"让回合静默消失。
- 回程代码（`next()` 之后的部分）只读：禁止 writeLeaf，只允许读取与日志。否则先执行插件的回程会把后续插件尚未提交的假设写进叶子。
- 每个中间件单次执行、单次合并。内核按槽位收窄上下文。钩子拿不到完整状态对象。回合上下文向每个槽位提供固定的只读元信息，包含当前回合数。
- 插件可以声明自定义槽位。自定义槽位必须声明挂载在六槽位的哪一刻之下执行，并且必须声明错误级别（关键或后台），不声明默认为后台。调度权在内核，插件之间不能互相驱动槽位。

### 6.4 commitBoundary 机制

commitBoundary 是引擎的最后一刻，内核自有，不开放注册。它按以下步骤运行：

1. 引擎确认前置各刻全部完成，blocking 后台任务已返回。
2. 引擎调用 commitLeaf。单事务执行：封存当前叶子、按 ephemeral 标记剥离仅限活跃叶子的字段、创建新叶子、移动 newest 指针。剥离只作用于封存进检查点的载荷，内核必须保证不损坏当前内存中的 ephemeral 字段值，活跃叶子的工作态不受影响。
3. 事务提交的瞬间触发 turnCreated。

它是"回合完整才封版"的执行点。引擎在此刻之前任何一点失败，叶子都只是未封版状态，历史链不产生脏节点。封版前不向插件发送任何通知。

### 6.5 输入形态的来源归属

本文档不硬编码输入类型枚举。各类输入的来源如下：

- 用户消息：界面层向 input 槽位投递。
- 斜杠命令：可静态识别的斜杠命令（如 `/clear`、`/roll`）由界面直接走命令总线，不产生回合。需要 AI 参与解释的命令进入 input 槽位，由正文生成插件加工，必要时以终局增量提前结束回合。
- 首轮开局：开局插件的 input 钩子在没有用户消息时自行产出开场输入。
- 重新生成：走 forkLeaf，不经过 input 槽位。
- 排队输入：回合进行中到达的输入由正文生成插件的队列字段承接，turnCreated 后由该插件经"投递输入"命令送入 input 槽位，见 14.2。排队不跳过 input 槽位。
- 提示词行为修饰（作者注、静默回合等）：正文生成插件的自有字段。正文生成插件在 preModel 从公共目录实时读取。

## 7. 状态归约器（内核职能）

变量系统不是插件，它是内核的状态归约器，本质上承担跨字段写入的调度职能。

- 归约器的输入是 AI 响应中的变量命令和回合初的状态快照。输出是跨字段的覆盖集，经 writeLeaf 的突变函数写入叶子。
- AI 的一切跨字段修改必须走归约器。插件禁止直接产出其他插件字段的值。
- 剧情编织插件对记忆与非玩家角色字段的剧情行写回，同样命令化并走归约器。该插件收缩为只写剧情编织与智库两个自有字段。
- 冲突处理：归约写入与用户手动修改按修改时序串行合并。归约结果不整体覆盖字段。用户在回合中途的手动修改不会被冲掉。
- 变量面板是归约器的可视化窗口。variableBatches 是归约历史日志，它是普通的叶子字段，随节点封存。
- 变量命令的解析、归约与提交维持 variableExecutor 管线。禁止绕过该管线直写状态。

## 8. 公共目录与实时读取

**领域状态的唯一真相是活跃叶子，公共目录是叶子载荷的实时读视图，不是第二份数据。** 服务句柄管的是状态之外的世界（AI、资产、日志、通知、设备存储），两者不竞争真相地位。句柄返回值在返回的那一刻没有状态归属，直到经钩子增量或 writeLeaf 落到叶子上，才进入公共目录的管辖。

- **读取规则**：插件在钩子执行的那一刻从公共目录读取其他插件的 public 字段。该规则覆盖用户在回合中途的手动修改。
- **读取的记忆化**：内核经 writeLeaf 漏斗掌握每个字段的变更时刻，天然持有字段版本号。公共目录的读取按字段版本做缓存与失效，与同一份依赖索引服务投影的精确失效（4.2）。
- **AI 响应不做指纹缓存**。模型调用是生产新数据，不是读取已存在状态，字段版本失效对它无意义。AI 历史需要留存时，由插件经 deviceStorage 自行记录。
- **快照的剩余用途**：回合初快照只服务两个用途——归约器的归约基准、重新生成的回退基准。快照不再冒充数据的当前形态。
- **推送规则**：内核应用一次 public 字段写入后推送字段名。推送只服务界面投影，不向插件推送。界面侧的精确失效机制见 4.2。
- **代价与纪律**：延迟读取使管线不再是严格无副作用的。系统把每次延迟读取的值记入调试信息。读取记录挂进回合上下文的 debugTrail，随恢复上下文存于活跃叶子，commitLeaf 时剥离。

## 9. 通信模型总表

分工口诀：读走公共目录，写走 writeLeaf，时机走事件，能力走服务句柄，界面动作走命令。

| 需求 | 通道 | 时效 |
| --- | --- | --- |
| 管线自产数据的传递（召回内容、提示词、解析结果） | 回合上下文，中间件链加工 draft，引擎合并 | 回合内冻结 |
| 跨插件读取领域数据 | 公共目录，执行时刻延迟读取，按字段版本记忆化 | 实时 |
| 写入自己的字段 | writeLeaf 突变函数，应用时刻求值 | 按时序合并 |
| 跨字段写入（AI 变量命令与剧情行） | 归约器，归约后走同一 writeLeaf | 按时序合并 |
| 界面刷新 | 投影依赖声明与切片推送；流式正文经对话管理插件过滤后走引擎私有投影通道 | 实时 |
| 异步插件唤醒 | 内核事件，只携带字段名 | 实时 |
| 对外部世界的动作（AI、资产、日志、通知、设备存储） | 服务句柄 | 即时 |
| 界面触发动作（含静态斜杠命令） | 命令总线 | 即时 |
| 重新生成与异常回退的基准 | 回合初快照 | 回合初冻结 |

## 10. 中断与恢复（两阶段 Savepoint 模型）

系统废弃逐阶段的断点探测。整个回合只有一个检查点：postLanding 正文落地。

### 10.1 三态 turnPhase

活跃叶子持有 turnPhase，只有三个值：

- `awaitingLanding`：正文落地之前。
- `settling`：正文落地之后、封版之前。
- 封版后无状态。

### 10.2 检查点之前中断

叶子只携带用户消息和前置阶段的少量写入。hydrate 读到 `awaitingLanding` 时，系统提供两个选项：重试本回合，或者撤销本回合（剥离残余写入）。语义简单，不需要恢复中间产出。

运行时失败与中断恢复共用同一入口。关键位抛错即引擎停止推进，叶子停留在 `awaitingLanding`，界面经 turnPhase 投影呈现"重试或撤销"两个命令，与刷新后 hydrate 的路径同构。错误细节经 devLog 句柄记录，需要用户可见的提示走"界面通知"服务句柄。

### 10.3 检查点之后中断

正文、回合计数、解析结果已经在 postLanding 一次原子写入。hydrate 读到 `settling` 时，系统按槽位整体重跑被打断的部分：

- **重跑粒度是六槽位本身**。settlement 是一个阶段，其内部任务不单独断点续跑；变量结算被打断，内核直接重新运行整个 settlement 槽位。backgroundJobs 同理。
- **纯计算槽位自动重跑**。结算所需的输入都可以从 chatHistory 和公共目录重算，系统不保存"中断前的已产出增量"。
- **含外部费用的槽位必须经用户确认后才重发**。生图等有外部费用的后台任务不静默重试，避免重复扣费。

重跑是用户可见的行为，恢复清单由活跃叶子的 turnPhase、恢复上下文与槽位注册表实时推导，作为引擎投影提供给界面，清单本身不持久化。确认动作走命令总线。

### 10.4 恢复上下文与字段归属

- 恢复上下文缩减为 assistantMessageId、userInput、解析结果，只服务 settling 态。
- turnPhase 与恢复上下文只属于 newest 指针指向的活跃叶子，以 ephemeral 标记声明。queueTasks 已划归正文生成插件，不再是内核 ephemeral 字段，见 14.2。
- 封版时，ephemeral 字段按 commitLeaf 流程剥离，不进入检查点。
- 系统不再使用独立的恢复日志存储库。
- 恢复流程完成时触发 recoveryCompleted 事件。

## 11. 会话标识与设备存储

### 11.1 会话标识

会话标识是内核保存在内存中的临时 ID。规则如下：

- 内核经存档管理器切换分支时重新生成该 ID。
- 常规回档（forkLeaf 回退到当前分支内的检查点）不改变会话标识。
- 会话标识不写入存档。刷新页面即产生新会话，此前在途的异步结果一律视为迟到。
- 同一线性链上的 commitLeaf 不触发 sessionEntered，非 blocking 任务在回合内持续有效。

### 11.2 设备存储（deviceStorage）

设备配置（deviceSetting）升级为设备存储（deviceStorage）。它是非存档级别的按设备持久化存储：

- 不进入数据树，不随 forkLeaf 与回档变化，不进入任何备份与同步包。
- 与叶子字段同构地分 public 与 private 两级，默认 private。
- 插件只能经设备存储服务句柄访问，见 13.5。
- 典型用途：界面偏好、迟到的异步结果暂存、AI 历史记录。存什么、何时通知、何时应用或丢弃，均为插件内部事务。

## 12. 内核不变量

| 编号 | 不变量 |
| --- | --- |
| K1 | sealed 节点不可变。commitLeaf 是产生 sealed 节点的唯一入口。回合未完成的叶子不封存。 |
| K2 | newest 指针恰好指向一个可写叶子。回合中途的写入只作用于该叶子。 |
| K3 | 仅限活跃叶子的字段以 ephemeral 标记声明，由 commitLeaf 流程在封版时剥离。系统不维护独立的字段清单，也不编写单字段的特殊判断。剥离只作用于封存载荷，不损坏内存中的 ephemeral 值。 |
| K4 | 回档类操作等于 forkLeaf。重新生成、回档、读取统一走分叉。系统不存在第二套回滚机制。 |
| K5 | hydrate 是唯一的水合入口。系统只在该入口执行归一化。 |
| K6 | 大体积二进制数据只以指针引用随节点克隆。资产持有引用计数。节点诞生加一，节点删除减一，归零即回收。 |
| K7 | 依赖方向单向。界面层依赖内核层。插件跨字段写入只经过归约器。 |
| K8 | 恢复信息只属于 newest 指向的活跃叶子。检查点不携带恢复上下文。 |
| K9 | 回合的启动、推进与封版由内核回合引擎独占。界面层只能投递输入，插件只能在槽位内加工。 |
| K10 | 界面层对内核层只有三种动作：调用命令、订阅投影、接收推送。命令不是新的写入通道。 |
| K11 | 会话标识只存在于内存，不写入存档。只有经存档管理器切换分支时才重新生成。 |
| K12 | 中断恢复的重跑粒度是六槽位整体。纯计算槽位自动重跑，含外部费用的槽位必须经用户确认。 |
| K13 | 领域状态的唯一真相是活跃叶子，公共目录是其读视图。服务句柄管状态之外的世界，句柄返回值经增量或 writeLeaf 落叶后才获得状态归属。 |
| K14 | 中间件链只存在于单个槽位内部。槽位短路只影响本槽位剩余钩子，回合级终局必须显式返回终局增量。回程代码禁止 writeLeaf。 |
| K15 | 插件的内核模块与界面模块之间只经投影、命令、推送通信。禁止共享内存、直接 import、私有事件。 |

---

# 第二部 子系统与插件实现层

## 13. 插件契约

每个插件导出四组声明（内核模块），可选附带界面模块（见第 2 节）。

### 13.1 fields

插件声明自己拥有的字段与归一化函数。字段有两个维度的标记：

- **可见性维度**：默认 private，可以标记为 public。public 字段允许其他插件引用；private 字段只允许所属模块与内核访问。
- **生命周期维度**：默认 permanent，可以标记为 ephemeral。ephemeral 字段仅限活跃叶子，由 commitLeaf 按标记剥离；permanent 字段随节点封存与继承。

内核从该声明生成归一化注册表、writeLeaf 的写入方校验规则和 commitLeaf 的剥离规则。校验在调用时强制，不依赖人工审查。

### 13.2 hooks

插件注册槽位钩子函数。槽位见第 6 节。执行契约见 6.3。

错误处理按槽位区分：

- preModel、modelCall、postLanding、settlement 是关键位。关键位抛错视同回合失败，走未封版恢复流程，错误记入 devLog。
- backgroundJobs 和事件回调属于后台类。后台类抛错时，内核捕获错误、记入 devLog 并跳过该插件，回合继续。
- 自定义槽位按注册时声明的错误级别归入以上两组，默认后台。

### 13.3 events

内核定义四个必要事件：

- **turnCreated**：commitLeaf 事务完成、新叶子就位。
- **sessionEntered**：新会话开始。订阅方据此校验会话身份，丢弃过期会话的异步结果。
- **recoveryCompleted**：中断回合的恢复流程完成。
- **storageOrganized**：手动整理完成。

插件可以注册自定义事件。订阅纪律不变：订阅方异步执行，只写入自己拥有的字段，必须校验会话身份。内核不持久化事件。一切事件（含自定义事件）只携带字段名，不携带数据。

中断检测不设事件。界面读取活跃叶子的 turnPhase 投影即可获知未完成的回合。

### 13.4 projections

插件提供只读派生数据。界面组件订阅这些数据。投影注册时必须声明依赖的 public 字段，用于精确失效。投影不声明界面挂载点，挂载关系由界面模块在界面层表达。

### 13.5 服务句柄

服务句柄是内核通过钩子上下文向插件注入的外部能力。它是插件接触外部世界的唯一方式。服务句柄在语义上对应 Rust 的 Trait：插件声明自己需要哪些能力，内核在调用时注入。插件没有声明的能力，在机制上就无法使用。

白名单五项：

1. AI 调用。
2. 资产读写。
3. devLog。
4. 界面通知。插件请求通知，界面层决定呈现方式。
5. 设备存储（deviceStorage）。非存档级持久化，规则见 11.2。

白名单登记进本文档。新增句柄必须修改本文档，并说明它为什么不属于读取、写入、事件三条通道。判断标准：产生状态归属的不能是服务，携带数据的不能是事件。

### 13.6 插件的禁止事项

- 禁止直接调用存储接口。
- 禁止写入其他插件的字段。
- 插件之间禁止直接调用。协作只经过槽位产出、公共目录或者事件。
- 禁止在钩子与事件回调之外持有跨回合的可变状态。跨回合的状态必须写入自己拥有的叶子字段。
- 插件字段禁止持有 nodeId 引用。跨回合引用只允许两种：资产指针和 turnId。本禁令已对照现状代码验证：当前业务模型没有任何字段持久化 nodeId，唯一的持久化 nodeId 属于存储引擎自身记录，按存储引擎内部细节豁免。
- 禁止自行 import 服务层。插件手里只有上下文注入的服务句柄。
- 插件的内核模块与界面模块之间禁止共享内存对象、直接 import、私有事件。通信只经投影、命令、推送。

### 13.7 无状态原则

插件保持无状态。插件每次获取数据都是一次无状态的读取动作。获知时机只有两种：槽位执行时刻从公共目录读取、内核事件唤醒后回公共目录重读。

系统不提供面向插件的变量变更订阅。未来出现必须即时反应的硬需求时，以新增一个具体语义的内核事件的方式解决，不恢复通用变量订阅。

## 14. 子系统划分与归属

### 14.1 业务子系统清单

插件层的业务子系统包括：记忆、智库、手机、非玩家角色、新闻、剧情、剧情编织、相册、正文生成、对话管理、开局。主对话界面是界面层组件，它投递输入、订阅投影，不编排任何东西。

### 14.2 输入队列（正文生成插件）

回合进行中到达的输入由正文生成插件自有的 permanent 队列字段（queueTasks）承接。规则如下：

- 内核不管理队列。队列如何保存、是否持久化、发送前是否执行 AI 扩展，均为插件内部事务。
- 队列字段是 permanent 字段，随节点封存，封版不清空。
- 正文生成插件订阅 turnCreated。回合封版后队列非空时，插件取出队首，调用"投递输入"命令启动下一回合。该输入照常经过 input 槽位。
- 队列字段归属正文生成插件，因为它是 input 槽位的主要消费者。

### 14.3 迟到异步结果（插件内部事务）

非 blocking 任务携带会话身份与目标叶子标识。任务迟到（会话过期或目标叶子不存在）时，内核不介入管理。插件自行决定处置方式，可用的能力包括：

- 经设备存储句柄暂存结果。
- 经界面通知句柄提示用户。
- 自行提供应用（经 writeLeaf 写回当前活跃叶子）或丢弃的逻辑。

### 14.4 对话管理插件

对话管理插件是 AI 回复进入界面与叶子的必经一站：

- 流式过滤：modelCall 产出的原始流先经该插件过滤（剥离 think 标签、敏感词替换、排版优化），再进入引擎私有投影通道。
- 落地解析：postLanding 写入 chatHistory 的文本以该插件的解析结果为准，界面看到的与叶子存下的是同一份过滤后文本。
- 流式处理者限定为该插件一家，不开放为过滤器链。多个过滤规则在该插件内部组合。

### 14.5 剧情编织插件的字段收缩

剧情编织插件只写剧情编织与智库两个自有字段。它对记忆与非玩家角色字段的剧情行写回命令化并走归约器，见第 7 节。

## 15. 删除清单

1. 删除 newestStory 覆盖集模型与回放逻辑。活跃叶子的载荷是工作区的唯一数据源。
2. 删除回合前状态快照机制。重新生成等于 forkLeaf 父节点。回合初快照只保留归约与回退两个基准用途。
3. 删除独立的恢复日志存储库。活跃叶子的恢复上下文承担恢复信息。
4. 删除三个独立的启动入口。系统统一使用 hydrate。
5. 删除 commitTurn 的合成逻辑与半提交防护。commitLeaf 收缩为单事务的四步。
6. 删除 baseCheckpointId 的对外身份职能。该字段降级为存储引擎的内部细节。
7. 删除钩子函数对完整状态对象的访问。内核按槽位收窄上下文。
8. 删除界面层对回合流程的编排。sendWorkflow 的驱动职责收归内核回合引擎。
9. 删除面向插件的公共目录变更推送。公共目录推送只服务界面投影。
10. 删除逐阶段的恢复断点探测。恢复模型压缩为两阶段 Savepoint，重跑粒度为六槽位整体。
11. 删除内核对输入队列的管理。queueTasks 划归正文生成插件的 permanent 字段。
12. 删除内核对迟到异步结果的处置逻辑。迟到结果的校验、暂存、通知与应用全部是插件内部事务。
13. 删除 AI 响应的指纹缓存设想。模型调用不缓存，历史留存走 deviceStorage。

## 16. 微服务理念的取舍

系统吸收两条思想：数据所有权（对应 fields 声明）和异步事件解耦（对应 events 订阅）。

系统拒绝微服务的基础设施：进程拆分、消息代理、事件溯源、调用网关、分布式锁。该应用是单标签页的单写入者前端，这些基础设施要解决的问题在这里不存在。

## 17. 从 SillyTavern 架构吸收的三条纪律

1. **通道唯一**：槽位解决执行顺序，事件解决异步通知，服务句柄解决外部能力，命令解决界面动作。系统不存在第五条耦合通道。事件与句柄的集合可以扩展，通道形态不变。
2. **上下文收窄**：插件钩子拿不到完整的状态对象。内核按槽位裁剪上下文。
3. **安全默认值做在通道里**：writeLeaf 的字段校验与 commitLeaf 的字段剥离在调用时强制。系统不依赖人工审查维持约定。

## 18. 远期方向（Rust 迁移形态）

本节内容是比 `[TARGET]` 更远的方向性记录，不影响当前 TypeScript 实现路线。

- 存储引擎、回合引擎、归约器与插件逻辑远期可用 Rust 实现。桌面端经 Tauri 编译，Web 端经 WASM 在 Web Worker 中运行。存储驱动按能力接口注入。
- 本文档的服务句柄、命令总线、投影声明、Savepoint 恢复均按与该形态同构的方式设计。迁移时只换语言与宿主，不换架构。
- 插件内核模块与界面模块之间禁止后门的纪律（K15）是 Rust 化的前置条件：迁移后跨模块通信必然跨越语言边界。
- 纯 Web 持久化的防御方向：启动时申请持久化权限、提供数据导出能力、利用不可变数据树的增量特性预留低成本同步接口。具体方案届时裁决。
- 跨端分发、P2P 同步、替换界面层为游戏引擎等设想仅作方向记录，均未裁决。

## 19. 明确不做的部分

- 系统不做运行时插件加载与第三方扩展机制。
- 系统不增加泛化槽位。六个槽位对应真实阶段边界。input 不是泛化槽位，它是全部输入的归一化入口。
- 系统不硬编码输入类型枚举。输入形态的差异由插件与树操作分别承担。
- 系统不在内核内建任何模型行为。AI 请求是插件的策略注入。
- 系统不重写提示词逻辑与模型适配层。这些逻辑作为插件的内部实现原样搬迁。
- 系统不提供面向插件的变量变更订阅。
- 系统不做逐阶段的中断恢复。恢复模型只有两阶段，重跑粒度是六槽位整体。
- 系统不静默重试有外部费用的任务。重跑必须经用户确认。
- 系统不做 AI 响应的指纹缓存。AI 历史需要留存时由插件经 deviceStorage 自行记录。
- 内核不管理输入队列，不介入迟到异步结果的处置。两者均为插件内部事务。
- 流式过滤不开放为插件间自由挂载的过滤器链。流式处理者限定为对话管理插件一家。
- URL 表现层的远期方案保留轻量路由库（wouter 为候选），隔离在翻译器接口之后。hydrate 已为路由层预留水合能力。
- 系统不改动增量编码、摘要目录、滚动保留等存储内部机制。
- 系统不维护独立的叶子专用字段清单。剥离规则由 fields 声明的 ephemeral 标记生成。
- 开发团队不推翻内核约束 L1 至 L12（定义于 `ideal_design.md` 约束表）。K1 至 K15 是这些约束的目标状态重述。

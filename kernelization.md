# 理想内核设计蓝图（第四版）

**文档定位**：本文档描述目标状态的设计。开发团队必须遵守 ideal_design.md 第三节的内核约束。本文档合并四组用户裁决。第一，系统采用内核加插件加应用层的形态。第二，系统保留中断回合的继续结算能力。第三，系统只吸收微服务的思想，不引入微服务的基础设施。第四，跨系统的领域数据引用采用实时读取，不采用回合快照。

---

## 1. 设计的两个基点

- **一个唯一数据源**：工作区是存档树中的叶子节点。该事实取代现状的三套并行机制。这三套机制是 newest 覆盖集、回合前状态快照、恢复运行日志。
- **一个统一契约**：业务子系统是插件。插件只通过内核声明的契约参与回合。插件不直接访问存储接口。插件不直接写入其他插件的字段。

## 2. 分层结构

系统包含三层。依赖方向单向。

1. 应用层包含界面组件与应用场景编排。组件是无逻辑的。
2. 插件层包含业务子系统。每个子系统是一个插件。子系统包含记忆、智库、手机、非玩家角色、新闻、剧情、剧情编织、相册。
3. 内核包含存储引擎、全局指针、树操作、会话管理、插件宿主、状态归约器。

应用层依赖插件层。插件层依赖内核。反向依赖禁止。插件在编译期静态注册。系统不提供运行时的扩展加载机制。

## 3. 内核的对外接口

内核对外提供五个树操作与一个全局指针。

| 操作 | 语义 |
| --- | --- |
| writeLeaf | 插件提交突变函数。内核在应用的那一刻对当前值求值。该规则防止管线写回覆盖用户在回合中途的手动修改。用户面板的手动编辑与管线写入走同一通道。内核把两者按时序串行化。 |
| commitLeaf | 系统封存当前叶子为检查点。系统分离叶子专用的字段。系统创建新叶子。系统移动指针。该操作是唯一的永久保存入口。该操作在单个事务内完成。 |
| forkLeaf | 系统从任意检查点分叉新叶子。系统移动指针。读取存档、回档、重新生成、覆盖都使用该操作。 |
| deleteNode | 系统级联修剪子树。指针落在被删集合时，系统把指针重定向到最近存活的祖先节点。现状已落实该操作。 |
| hydrate | 系统把节点转换为完整的领域状态。该操作是唯一的水合入口。启动、继续、中断恢复、未来的路由层都使用该操作。系统只在该入口执行归一化。 |

补充规则如下。

- 全局指针是一条单记录。记录包含 rootId 与 nodeId。指针指向当前活跃叶子。nodeId 是唯一的节点身份。
- 节点只有两种类别。leaf 类别的节点可写。sealed 类别的节点不可变。
- 系统对大体积二进制数据维持指针化存储。系统写入时分离二进制数据。系统读取时填充数据。
- 增量编码与摘要目录是存储引擎的内部细节。上层模块不感知这些细节。

## 4. 状态归约器（内核职能，原变量系统）

变量系统不是插件。变量系统是内核的状态归约器。现状的 variableOverrides 单次产出覆盖八个字段。该事实证明变量系统是游戏状态的化身，不是普通业务子系统。

- 归约器的输入是 AI 响应中的变量命令与回合初的状态快照。归约器的输出是跨字段的覆盖集。覆盖集经 writeLeaf 的突变函数写入叶子。
- AI 的一切跨字段修改必须走归约器。插件禁止直接产出其他插件字段的值。
- 推论：剧情编织插件对记忆与非玩家角色字段的剧情行写回，同样命令化并走归约器。该插件因此收缩为只写剧情编织与智库两个自有字段。
- 变量面板是归约器的可视化窗口。variableBatches 是归约历史日志。变量面板不是业务子系统。
- 变量命令的解析、归约与提交维持现状的 variableExecutor 管线。开发团队禁止绕过该管线直写状态。

## 5. 插件契约

每个插件导出四组声明。

1. fields：插件声明自己拥有的叶子字段与归一化函数。字段分为两级。public 字段允许其他插件引用。private 字段只允许所属模块与内核访问。内核从该声明生成三份产物。第一份产物是叶子专用的字段清单。第二份产物是 hydrate 使用的归一化注册表。第三份产物是 writeLeaf 的写入方校验规则。校验在调用时强制，不依赖人工审查。
2. hooks：插件注册槽位钩子函数。槽位固定为五个。五个槽位是 preModel、postLanding、settlement、backgroundJobs、commitBoundary。五个槽位对应现状的真实阶段边界。系统不增加泛化的槽位。内核按槽位收窄上下文。钩子函数拿不到完整的状态对象。每个槽位只暴露该槽位必需的能力。
3. events：插件订阅回合事件。事件只有三个。三个事件是 turnCommitted、sessionEntered、leafDeleted。订阅方异步执行。订阅方只写入自己拥有的字段。订阅方必须校验会话身份。过期会话的异步结果必须丢弃。内核不持久化事件。事件只携带字段名，不携带数据。订阅方收到通知后回公共目录重读。订阅方在下次执行时以自己字段为准做结果一致的检查。
4. projections：插件提供只读派生数据。界面组件订阅这些数据。

插件的禁止事项如下。

- 插件禁止直接调用存储接口。
- 插件禁止写入其他插件的字段。跨字段写入只能以变量命令的形式提交给归约器。
- 插件之间禁止直接调用。插件协作只经过槽位产出、公共目录或者事件。
- 插件禁止在钩子函数与事件回调之外持有跨回合的可变状态。跨回合的状态必须写入自己拥有的叶子字段。

## 6. 公共目录与实时读取

内核为 public 字段维护实时公共目录。公共目录是叶子载荷的实时视图。公共目录不是第二份数据。

- 读取规则：插件在钩子执行的那一刻从公共目录读取其他插件的 public 字段。该规则覆盖用户在回合中途的手动修改。用户在生成之前或者生成之中修改了非玩家角色档案、智库条目或者设置时，后续钩子读到的是新值。
- 快照的剩余用途：回合初快照只服务两个用途。第一个用途是归约器的归约基准。第二个用途是重新生成的回退基准。快照不再冒充数据的当前形态。
- 推送规则：内核应用一次 public 字段写入后，向订阅者推送字段名。订阅者包含界面投影与异步插件。订阅者收到通知后回公共目录重读。
- 代价与纪律：延迟读取使管线不再是严格无副作用的。组提示词时与结算时读到的数据可能不同。系统把每次延迟读取的值记入调试信息。该记录保证事后可以复盘钩子当时读到的内容。

## 7. 通信模型总表

| 需求 | 通道 | 时效 |
| --- | --- | --- |
| 管线自产数据的传递（提示词、解析结果） | 回合上下文与增量对象，不可变 | 回合内冻结 |
| 跨插件读取领域数据 | 公共目录，执行时刻延迟读取 | 实时 |
| 写入自己的字段 | writeLeaf 突变函数，应用时刻求值 | 实时合并 |
| 跨字段写入（AI 变量命令与剧情行） | 归约器，归约后走同一 writeLeaf | 实时合并 |
| 界面刷新与异步插件唤醒 | 事件推送，只携带字段名 | 实时 |
| 重新生成与异常回退的基准 | 回合初快照 | 回合初冻结 |

## 8. 执行顺序

回合的执行顺序来自子系统的真实数据依赖调研。调研结论如下。

- **强制串行链**：归约相位、非玩家角色结算、剧情编织结算必须依次执行。非玩家角色结算消费归约后的数据。剧情编织结算消费前两者的产出。该顺序是正确性要求，不是编排偏好。
- **可并行组**：记忆结算、世界与旅人结算、正文生图三者互不依赖。三者都只依赖回复落地的产出。正文生图是全管线最自由的任务。
- **后台任务组**：新闻、忆庭入库、手机种子、正文生图四个任务互相独立。各任务的入参全部快照化后，四个任务恒定并行。系统移除现状的串行与并行双模式。
- **模型前置阶段**：忆庭召回与智库召回互相独立，维持并行。开局新闻预处理必须在提示词组装之前完成。
- **剧情编织结算是全管线最大的汇聚点**。该插件读取归约相位、非玩家角色结算与记忆结算的全部产出。字段所有权划分时必须最谨慎地处理该插件。

## 9. 中断与恢复

- 内核的会话层持有 queueTasks 与 turnPhase 两个字段。这两个字段是叶子专用的字段。
- 系统在回合的每个阶段边界更新 turnPhase。
- hydrate 读到未完成的 turnPhase 时，系统提供继续结算或者放弃两个选项。继续结算复用同一个尾段函数。
- 系统删除独立的恢复日志存储库。叶子的 turnPhase 字段与最小恢复上下文承担恢复信息。最小恢复上下文包含 assistantMessageId 与 userInput。

## 10. 内核不变量

| 编号 | 不变量 |
| --- | --- |
| K1 | sealed 节点不可变。commitLeaf 是产生 sealed 节点的唯一入口。 |
| K2 | 每个全局指针恰好有一个可写叶子。回合中途的写入只作用于该叶子。 |
| K3 | 内核按字段所有权清单分离叶子专用的字段。开发团队禁止编写单字段的特殊判断。 |
| K4 | 回档类操作等于 forkLeaf。系统不存在第二套回滚机制。 |
| K5 | hydrate 是唯一的水合入口。系统只在该入口执行归一化。 |
| K6 | 大体积二进制数据只以指针引用随节点克隆。 |
| K7 | 依赖方向单向。应用层依赖插件层。插件层依赖内核。插件跨字段写入只经过归约器。 |

## 11. 删除清单

开发团队在目标状态中删除以下机制。每项删除直接降低复杂度。

1. 删除 newestStory 覆盖集模型与回放逻辑。叶子的载荷就是工作区的唯一数据源。
2. 删除回合前状态快照机制。重新生成等于 forkLeaf 父节点。回合初快照只保留归约与回退两个基准用途。
3. 删除独立的恢复日志存储库。叶子的 turnPhase 字段承担恢复信息。
4. 删除三个独立的启动入口。系统统一使用 hydrate。
5. 删除 commitTurn 的合成逻辑与半提交防护。commitLeaf 收缩为单事务的四步。
6. 删除 baseCheckpointId 的对外身份职能。该字段降级为存储引擎的内部细节。
7. 删除钩子函数对完整状态对象的访问。内核按槽位收窄上下文。

## 12. 微服务理念的取舍

系统吸收微服务的两条思想。第一条思想是数据所有权。该思想对应 fields 声明。第二条思想是异步事件解耦。该思想对应 events 订阅。

系统拒绝微服务的基础设施。拒绝清单包含进程拆分、消息代理、事件溯源、调用网关、分布式锁。该应用是单标签页的单写入者前端。这些基础设施要解决的问题在该应用中不存在。

## 13. 从 SillyTavern 架构吸收的三条纪律

1. 通道唯一。槽位解决执行顺序。事件解决异步通知。系统不存在第三条耦合通道。
2. 上下文收窄。插件钩子拿不到完整的状态对象。内核按槽位裁剪上下文。
3. 安全默认值做在通道里。writeLeaf 的字段校验与 commitLeaf 的字段分离在调用时强制。系统不依赖人工审查维持约定。

## 14. 明确不做的部分

- 系统不做运行时插件加载与第三方扩展机制。ideal_design.md 第七节已拒绝该方案。
- 系统不增加泛化槽位。五个槽位对应现状的真实阶段边界。
- 系统不重写提示词逻辑与模型适配层。这些逻辑作为插件的内部实现原样搬迁。
- 系统不引入路由库。hydrate 已经为路由层预留接口。
- 系统不改动增量编码、摘要目录、滚动保留等存储内部机制。
- 开发团队不推翻内核约束 L1 至 L12。K1 至 K7 是这些约束的目标状态重述。

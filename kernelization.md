# 理想内核设计蓝图（第五版）

**文档定位**：本文档描述目标状态的设计。执行边界以 `AGENTS.md` 为准。当前事实与差距以 `ideal_design.md` 为准。本文档全部内容属于 `[TARGET]`，不描述当前已经具备的行为。

**远期方向**：本文档的实现按与未来 Rust 核心同构的形态设计。能力句柄对应 Rust 的 Trait，命令总线对应 IPC invoke，投影对应 Selector，恢复对应 Savepoint。当前实现语言保持 TypeScript。Rust、Tauri、WASM、OPFS 属于远期迁移形态，见第 15 节。当前的增量重构路线图不受本文档影响。

**本版变更**：本版按 2026-08-10 至 2026-08-11 的设计讨论整体重写。主要变化如下：

1. 回合的所有权收归内核。内核持有回合引擎，执行顺序由引擎状态机硬编码。
2. 槽位扩展为六个：input、preModel、postLanding、settlement、backgroundJobs、commitBoundary。input 是引擎的唯一入口。
3. AI 请求插件化。引擎只定义 modelCall 扩展点，不内建任何模型行为。
4. 界面层与内核层之间采用命令制交互。插件可以注册命令，界面按命令名调用。
5. 中断恢复压缩为两阶段 Savepoint 模型，废弃逐阶段断点探测。
6. 全局指针明确为单一 newest 指针，服务快速启动。
7. 废除独立的叶子专用字段清单。剥离规则直接写入 commitLeaf 流程。
8. 事件系统开放。内核定义四个必要事件，插件可以注册自定义事件。裁减 leafDeleted。
9. 新增资产引用计数回收机制与手动整理入口。
10. 取消面向插件的公共目录变更推送。公共目录推送只服务界面投影。
11. 重新生成与顶替明确归入 forkLeaf，不经过 input 槽位。
12. 界面层保持无状态，只允许白名单内的临时本地状态。

---

## 1. 设计哲学

以下五条原则是系统的最高裁决依据。任何实现不得走违反这些原则的捷径。

1. **命令与查询分离，界面极度无脑化**。界面不知道底层存储结构，不订阅具体变量名，不自己修改状态。界面只是内核状态在某一刻的只读投影：`View = f(StateSlice)`。
2. **插件是客，内核是主**。插件不能直接使用系统的基础设施。控制权完全倒置。插件没有自己的生命周期，没有常驻的私有订阅。插件只在回合引擎指定的槽位中被唤醒，执行完毕立即交出控制权。
3. **回合即事务**。执行管线无论多长，本质上是一次状态跃迁。系统设立绝对的安全检查点。事务只有两个结局：没抵达检查点则回滚丢弃，抵达了检查点则下游逻辑可重试。系统不做"从第几步中断处继续"的脆弱设计。
4. **数据树指针化与结构共享**。系统拒绝深度拷贝与直接覆写。一切状态变动都是在不可变的状态树上长出新叶子并移动头指针。这为低开销的状态比对和时间旅行提供物理基础。
5. **跨越环境边界**。核心业务逻辑不与宿主环境耦合。一切副作用操作抽象为能力接口。切换实现即可切换运行环境。

## 2. 分层结构

系统包含两层，依赖方向单向：

1. **界面层**包含界面组件和场景编排。组件是无逻辑的。
2. **内核层**包含存储引擎、全局指针、树操作、回合引擎、插件宿主、状态归约器和全部插件。内核插件属于内核层。

界面层依赖内核层。反向依赖被禁止。插件在编译期静态注册，系统不提供运行时扩展加载。

插件层的业务子系统包括：记忆、智库、手机、非玩家角色、新闻、剧情、剧情编织、相册。主对话界面是界面层组件。它投递输入、订阅投影，不编排任何东西。

### 2.1 界面层的无状态纪律

界面层保持无状态。组件只允许持有白名单内的本地临时状态：

1. 未提交的草稿，例如输入框文本、表单编辑中的值。
2. 纯视觉瞬态，例如悬停、焦点、动画进行中、滚动位置。
3. 手势与交互进行中，例如拖拽中。
4. 一次性提示，例如 toast。

白名单之外的界面状态被禁止。面板展开折叠这类偏好属于设备设置，走持久化，不算本地临时状态。导航状态 view 是界面层自有的瞬态，不进内核，不进叶子。

## 3. 界面层与内核层的交互

界面层对内核层只有三种动作：**调用命令、订阅投影、接收推送**。

### 3.1 命令总线

- 内核注册内建命令：投递输入、writeLeaf、forkLeaf、deleteNode、hydrate、手动整理。
- 插件注册自己的用例命令，例如相册的生成图像。界面按命令名直接调用。
- 命令注册在编译期静态完成。
- 命令是调用壳，不是新的写入通道。命令内部的写入只能经 writeLeaf 和归约器。命令不允许绕开字段校验。
- 命令只触发动作。命令的执行结果一律经投影回流到界面，命令不直接返回业务数据。

### 3.2 投影订阅与精确失效

- 插件在注册投影函数时，必须显式声明它依赖的 public 字段。
- 全部写入都经过 writeLeaf 这一个漏斗。内核在应用写入的那一刻知道哪个字段变了。
- 内核根据依赖声明维护字段到投影的反向索引。字段变脏时，内核只重算依赖该字段的投影，并把重算后的界面切片推送给订阅者。
- 界面收到推送后更新对应组件。界面不缓存投影数据本体。

### 3.3 手动编辑的写回时机

手动编辑只在用户点击保存时触发一次 writeLeaf。系统不做失焦自动写入，不做逐键写入。未保存的草稿是白名单内的本地状态。编辑期间管线读到的是叶子里的旧值。

## 4. 内核的对外接口

### 4.1 五个树操作

| 操作 | 语义 |
| --- | --- |
| writeLeaf | 提交突变函数。内核在应用的那一刻对当前值求值。用户面板的手动编辑与管线写入走同一通道，内核按修改时序把两者串行化。任何一方都不整体覆盖字段。钩子执行过程中允许多次 writeLeaf。 |
| commitLeaf | 封存当前叶子为检查点，创建新叶子，移动指针。这是唯一的永久保存入口，在单个事务内完成。它只在回合完整结束时由引擎在 commitBoundary 调用。回合未完成的叶子不封存，也不会成为父节点。封版时按流程剥离仅限活跃叶子的字段。 |
| forkLeaf | 从任意检查点分叉新叶子并移动指针。读取存档、回档、重新生成、顶替都使用该操作。重新生成与顶替不经过 input 槽位。 |
| deleteNode | 级联修剪子树。指针落在被删集合时，重定向到最近存活的祖先节点。删除时对被删子树的每个节点解析资产引用并逐个递减引用计数。 |
| hydrate | 把节点转换为完整的领域状态。这是唯一的水合入口，系统只在这里执行归一化。启动、继续、中断恢复都使用该操作。 |

### 4.2 全局指针

系统只保留一个 newest 指针。它是一条单记录，包含 rootId 与 nodeId，指向当前活跃叶子。它的职责是让系统启动时快速定位工作区。

### 4.3 资产服务与引用计数

大体积二进制数据维持指针化存储。存档只持有指针引用。写入时分离二进制数据，读取时填充。

内核向插件暴露资产服务句柄：写入资产、读取资产、删除资产。插件只能通过句柄操作资产。

引用计数规则对称，共三个落点：

1. 任何节点诞生时，它携带的全部资产引用计数加一。writeAsset 创建资产时归属当前叶子，计数加一。commitLeaf 创建新叶子时，新叶子继承父节点字段，继承的引用计数加一。
2. 叶子封存为检查点是同一节点的身份转变。引用关系不变，计数不动。
3. deleteNode 删除节点时，对被删子树的每个节点解析引用并逐个减一。计数归零的资产由系统回收。

### 4.4 手动整理入口

系统提供一个手动触发的一键整理功能。它的职责方向包括：把没有父节点的散落存档点整理为根节点、回收孤儿资产、校正资产引用计数。整理功能复用资产服务句柄实现。具体范围在实现时确定。

### 4.5 存储内部细节

增量编码与摘要目录是存储引擎的内部细节。上层模块不感知。

## 5. 回合引擎与六槽位

### 5.1 所有权

回合引擎是内核的职能。界面层向 input 槽位投递输入，这是界面层影响回合的唯一方式。引擎看到输入后启动回合，按状态机逐刻推进，自己调用 commitLeaf 封版。启动时机、执行顺序、封版时机全部在内核手里。

### 5.2 六槽位

回合从一次 turnCreated 到下一次 turnCreated，经过六刻：

1. **input**：引擎的唯一入口。产出契约只有一条：槽位结束时固定输入变量必须有合法值，否则判回合失败。输入的加工是插件的内部事务。
2. **preModel**：模型前置。召回类任务在此并行。提示词构建是正文生成插件的内部实现，注册在此槽位。
3. **modelCall 扩展点**：preModel 与 postLanding 之间的策略注入点。AI 插件以策略形式注册，负责实际请求、重试与解析。引擎只做流式投影与中断控制。流式文本走引擎的私有投影通道，界面直接订阅引擎，不经过事件和公共目录。
4. **postLanding**：正文落地。正文写入 chatHistory，回合计数递增。这是回合的唯一检查点（Savepoint）。
5. **settlement**：结算。归约相位、非玩家角色结算、剧情编织结算强制串行。记忆结算、世界与旅人结算与之并行。
6. **backgroundJobs**：后台任务。插件注册钩子时声明 blocking 级别。引擎在 commitBoundary 之前只等待 blocking 钩子。非 blocking 任务携带会话身份与目标叶子标识，完成后 writeLeaf 到活跃叶子。

commitBoundary 见 5.4。

### 5.3 槽位的统一执行契约

六个槽位执行同一套契约：

- 钩子按编译期注册表的顺序执行。
- 钩子返回不可变增量对象，引擎合并进回合上下文。每个钩子单次返回、单次合并。回合上下文是引擎持有的内存对象，不落叶子。需要渐进产出的数据走 writeLeaf 写自有字段。
- 内核按槽位收窄上下文。钩子拿不到完整状态对象。回合上下文向每个槽位提供固定的只读元信息，包含当前回合数。
- 插件可以声明自定义槽位。自定义槽位必须声明挂载在六槽位的哪一刻之下执行。调度权在内核，插件之间不能互相驱动槽位。

### 5.4 commitBoundary 机制

commitBoundary 是引擎的最后一刻，内核自有，不开放注册。它按以下步骤运行：

1. 引擎确认前置各刻全部完成，blocking 后台任务已返回。
2. 引擎调用 commitLeaf。单事务执行：封存当前叶子、剥离仅限活跃叶子的字段（queueTasks、turnPhase、恢复上下文）、创建新叶子、移动 newest 指针。
3. 事务提交的瞬间触发 turnCreated。

它是"回合完整才封版"的执行点。引擎在此刻之前任何一点失败，叶子都只是未封版状态，历史链不产生脏节点。封版前不向插件发送任何通知。

### 5.5 输入形态的来源归属

本文档不硬编码输入类型枚举。各类输入的来源如下：

- 用户消息：界面层向 input 槽位投递。
- 首轮开局：开局插件的 input 钩子在没有用户消息时自行产出开场输入。
- 重新生成与顶替：走 forkLeaf，不经过 input 槽位。
- 提示词行为修饰（作者注、静默回合等）：正文生成插件的自有字段。正文生成插件在 preModel 从公共目录实时读取。

## 6. 状态归约器（内核职能）

变量系统不是插件，它是内核的状态归约器，本质上承担跨字段写入的调度职能。

- 归约器的输入是 AI 响应中的变量命令和回合初的状态快照。输出是跨字段的覆盖集，经 writeLeaf 的突变函数写入叶子。
- AI 的一切跨字段修改必须走归约器。插件禁止直接产出其他插件字段的值。
- 剧情编织插件对记忆与非玩家角色字段的剧情行写回，同样命令化并走归约器。该插件收缩为只写剧情编织与智库两个自有字段。
- 冲突处理：归约写入与用户手动修改按修改时序串行合并。归约结果不整体覆盖字段。用户在回合中途的手动修改不会被冲掉。
- 变量面板是归约器的可视化窗口。variableBatches 是归约历史日志，它是普通的叶子字段，随节点封存。
- 变量命令的解析、归约与提交维持 variableExecutor 管线。禁止绕过该管线直写状态。

## 7. 插件契约

每个插件导出四组声明。

### 7.1 fields

插件声明自己拥有的字段与归一化函数。字段分两级。public 字段允许其他插件引用。private 字段只允许所属模块与内核访问。内核从该声明生成归一化注册表和 writeLeaf 的写入方校验规则。校验在调用时强制，不依赖人工审查。

### 7.2 hooks

插件注册槽位钩子函数。槽位见第 5 节。执行契约见 5.3。

错误处理按槽位区分：

- preModel、modelCall、postLanding、settlement 是关键位。关键位抛错视同回合失败，走未封版恢复流程，错误记入 devLog。
- backgroundJobs 和事件回调属于后台类。后台类抛错时，内核捕获错误、记入 devLog 并跳过该插件，回合继续。

### 7.3 events

内核定义四个必要事件：

- **turnCreated**：commitLeaf 事务完成、新叶子就位。
- **sessionEntered**：新会话开始。订阅方据此校验会话身份，丢弃过期会话的异步结果。
- **recoveryCompleted**：中断回合的恢复流程完成。
- **storageOrganized**：手动整理完成。

插件可以注册自定义事件。订阅纪律不变：订阅方异步执行，只写入自己拥有的字段，必须校验会话身份。内核不持久化事件。事件只携带字段名，不携带数据。

中断检测不设事件。界面读取活跃叶子的 turnPhase 投影即可获知未完成的回合。

### 7.4 projections

插件提供只读派生数据。界面组件订阅这些数据。投影注册时必须声明依赖的 public 字段，用于精确失效。

### 7.5 服务句柄

服务句柄是内核通过钩子上下文向插件注入的外部能力。它是插件接触外部世界的唯一方式。服务句柄在语义上对应 Rust 的 Trait：插件声明自己需要哪些能力，内核在调用时注入。插件没有声明的能力，在机制上就无法使用。

初版白名单四项：

1. AI 调用。
2. 资产读写。
3. devLog。
4. 界面通知。插件请求通知，界面层决定呈现方式。

白名单登记进本文档。新增句柄必须修改本文档，并说明它为什么不属于读取、写入、事件三条通道。判断标准：产生状态归属的不能是服务，携带数据的不能是事件。

### 7.6 插件的禁止事项

- 禁止直接调用存储接口。
- 禁止写入其他插件的字段。
- 插件之间禁止直接调用。协作只经过槽位产出、公共目录或者事件。
- 禁止在钩子与事件回调之外持有跨回合的可变状态。跨回合的状态必须写入自己拥有的叶子字段。
- 插件字段禁止持有 nodeId 引用。跨回合引用只允许两种：资产指针和 turnId。本禁令已对照现状代码验证：当前业务模型没有任何字段持久化 nodeId，唯一的持久化 nodeId 属于存储引擎自身记录，按存储引擎内部细节豁免。
- 禁止自行 import 服务层。插件手里只有上下文注入的服务句柄。

### 7.7 无状态原则

插件保持无状态。插件每次获取数据都是一次无状态的读取动作。获知时机只有两种：槽位执行时刻从公共目录读取、内核事件唤醒后回公共目录重读。

系统不提供面向插件的变量变更订阅。未来出现必须即时反应的硬需求时，以新增一个具体语义的内核事件的方式解决，不恢复通用变量订阅。

## 8. 公共目录与实时读取

内核为 public 字段维护实时公共目录。公共目录是叶子载荷的实时视图，不是第二份数据。

- **读取规则**：插件在钩子执行的那一刻从公共目录读取其他插件的 public 字段。该规则覆盖用户在回合中途的手动修改。
- **快照的剩余用途**：回合初快照只服务两个用途——归约器的归约基准、重新生成的回退基准。快照不再冒充数据的当前形态。
- **推送规则**：内核应用一次 public 字段写入后推送字段名。推送只服务界面投影，不向插件推送。界面侧的精确失效机制见 3.2。
- **代价与纪律**：延迟读取使管线不再是严格无副作用的。系统把每次延迟读取的值记入调试信息。读取记录挂进回合上下文的 debugTrail，随恢复上下文存于活跃叶子，commitLeaf 时剥离。

## 9. 通信模型总表

分工口诀：读走公共目录，写走 writeLeaf，时机走事件，能力走服务句柄，界面动作走命令。

| 需求 | 通道 | 时效 |
| --- | --- | --- |
| 管线自产数据的传递（召回内容、提示词、解析结果） | 回合上下文，钩子返回增量，引擎合并 | 回合内冻结 |
| 跨插件读取领域数据 | 公共目录，执行时刻延迟读取 | 实时 |
| 写入自己的字段 | writeLeaf 突变函数，应用时刻求值 | 按时序合并 |
| 跨字段写入（AI 变量命令与剧情行） | 归约器，归约后走同一 writeLeaf | 按时序合并 |
| 界面刷新 | 投影依赖声明与切片推送；流式正文走引擎私有投影通道 | 实时 |
| 异步插件唤醒 | 内核事件，只携带字段名 | 实时 |
| 对外部世界的动作（AI、资产、日志、通知） | 服务句柄 | 即时 |
| 界面触发动作 | 命令总线 | 即时 |
| 重新生成与异常回退的基准 | 回合初快照 | 回合初冻结 |

## 10. 中断与恢复（两阶段 Savepoint 模型）

系统废弃逐阶段的断点探测。整个回合只有一个检查点：postLanding 正文落地。

### 10.1 三态 turnPhase

活跃叶子持有 turnPhase，只有三个值：

- `awaitingLanding`：正文落地之前。
- `settling`：正文落地之后、封版之前。
- 封版后无状态。

### 10.2 检查点之前中断

叶子只携带用户消息和前置阶段的少量写入。hydrate 读到 `awaitingLanding` 时，系统提供两个选项：重试本回合，或者撤销本回合（剥离残余写入）。语义简单，不需要恢复中间产出。

### 10.3 检查点之后中断

正文、回合计数、解析结果已经在 postLanding 一次原子写入。hydrate 读到 `settling` 时，系统向用户列出可重跑的任务清单，由用户逐项确认。清单至少包含两类：

1. 主线对话继续：重新执行 settlement 槽位。
2. 生图等有外部费用的后台任务：逐项确认后才重发。

重跑是用户确认后的行为，系统不静默重试。这避免了外部付费任务的重复扣费。结算所需的输入都可以从 chatHistory 和公共目录重算，系统不保存"中断前的已产出增量"。

### 10.4 恢复上下文与字段归属

- 恢复上下文缩减为 assistantMessageId、userInput、解析结果，只服务 settling 态。
- queueTasks、turnPhase、恢复上下文只属于 newest 指针指向的活跃叶子。queueTasks 是待消费输入的队列。
- 封版时，这些字段按 commitLeaf 流程剥离，不进入检查点。
- 系统不再使用独立的恢复日志存储库。
- 恢复流程完成时触发 recoveryCompleted 事件。

## 11. 内核不变量

| 编号 | 不变量 |
| --- | --- |
| K1 | sealed 节点不可变。commitLeaf 是产生 sealed 节点的唯一入口。回合未完成的叶子不封存。 |
| K2 | newest 指针恰好指向一个可写叶子。回合中途的写入只作用于该叶子。 |
| K3 | 仅限活跃叶子的字段由 commitLeaf 流程在封版时剥离。系统不维护独立的字段清单，也不编写单字段的特殊判断。 |
| K4 | 回档类操作等于 forkLeaf。重新生成、顶替、回档、读取统一走分叉。系统不存在第二套回滚机制。 |
| K5 | hydrate 是唯一的水合入口。系统只在该入口执行归一化。 |
| K6 | 大体积二进制数据只以指针引用随节点克隆。资产持有引用计数。节点诞生加一，节点删除减一，归零即回收。 |
| K7 | 依赖方向单向。界面层依赖内核层。插件跨字段写入只经过归约器。 |
| K8 | 恢复信息只属于 newest 指向的活跃叶子。检查点不携带恢复上下文。 |
| K9 | 回合的启动、推进与封版由内核回合引擎独占。界面层只能投递输入，插件只能在槽位内加工。 |
| K10 | 界面层对内核层只有三种动作：调用命令、订阅投影、接收推送。命令不是新的写入通道。 |

## 12. 删除清单

1. 删除 newestStory 覆盖集模型与回放逻辑。活跃叶子的载荷是工作区的唯一数据源。
2. 删除回合前状态快照机制。重新生成等于 forkLeaf 父节点。回合初快照只保留归约与回退两个基准用途。
3. 删除独立的恢复日志存储库。活跃叶子的恢复上下文承担恢复信息。
4. 删除三个独立的启动入口。系统统一使用 hydrate。
5. 删除 commitTurn 的合成逻辑与半提交防护。commitLeaf 收缩为单事务的四步。
6. 删除 baseCheckpointId 的对外身份职能。该字段降级为存储引擎的内部细节。
7. 删除钩子函数对完整状态对象的访问。内核按槽位收窄上下文。
8. 删除界面层对回合流程的编排。sendWorkflow 的驱动职责收归内核回合引擎。
9. 删除面向插件的公共目录变更推送。公共目录推送只服务界面投影。
10. 删除逐阶段的恢复断点探测。恢复模型压缩为两阶段 Savepoint。

## 13. 微服务理念的取舍

系统吸收两条思想：数据所有权（对应 fields 声明）和异步事件解耦（对应 events 订阅）。

系统拒绝微服务的基础设施：进程拆分、消息代理、事件溯源、调用网关、分布式锁。该应用是单标签页的单写入者前端，这些基础设施要解决的问题在这里不存在。

## 14. 从 SillyTavern 架构吸收的三条纪律

1. **通道唯一**：槽位解决执行顺序，事件解决异步通知，服务句柄解决外部能力，命令解决界面动作。系统不存在第五条耦合通道。事件与句柄的集合可以扩展，通道形态不变。
2. **上下文收窄**：插件钩子拿不到完整的状态对象。内核按槽位裁剪上下文。
3. **安全默认值做在通道里**：writeLeaf 的字段校验与 commitLeaf 的字段剥离在调用时强制。系统不依赖人工审查维持约定。

## 15. 远期方向（Rust 迁移形态）

本节内容是比 `[TARGET]` 更远的方向性记录，不影响当前 TypeScript 实现路线。

- 存储引擎、回合引擎、归约器与插件逻辑远期可用 Rust 实现。桌面端经 Tauri 编译，Web 端经 WASM 在 Web Worker 中运行。存储驱动按能力接口注入。
- 本文档的服务句柄、命令总线、投影声明、Savepoint 恢复均按与该形态同构的方式设计。迁移时只换语言与宿主，不换架构。
- 纯 Web 持久化的防御方向：启动时申请持久化权限、提供数据导出能力、利用不可变数据树的增量特性预留低成本同步接口。具体方案届时裁决。
- 跨端分发、P2P 同步、替换界面层为游戏引擎等设想仅作方向记录，均未裁决。

## 16. 明确不做的部分

- 系统不做运行时插件加载与第三方扩展机制。
- 系统不增加泛化槽位。六个槽位对应真实阶段边界。input 不是泛化槽位，它是全部输入的归一化入口。
- 系统不硬编码输入类型枚举。输入形态的差异由插件与树操作分别承担。
- 系统不在内核内建任何模型行为。AI 请求是插件的策略注入。
- 系统不重写提示词逻辑与模型适配层。这些逻辑作为插件的内部实现原样搬迁。
- 系统不提供面向插件的变量变更订阅。
- 系统不做逐阶段的中断恢复。恢复模型只有两阶段。
- 系统不静默重试有外部费用的任务。重跑必须经用户确认。
- URL 表现层的远期方案保留轻量路由库（wouter 为候选），隔离在翻译器接口之后。hydrate 已为路由层预留水合能力。
- 系统不改动增量编码、摘要目录、滚动保留等存储内部机制。
- 系统不维护独立的叶子专用字段清单。剥离规则直接写在 commitLeaf 流程里。
- 开发团队不推翻内核约束 L1 至 L12。K1 至 K10 是这些约束的目标状态重述。

# KaiTuoYiShi 内核

回合制 AI 角色扮演应用的目标内核架构：不可变数据树 + 回合引擎 + 插件宿主。本词汇表定义 `kernelization.md` 使用的领域语言。

## Language

**叶子 (leaf)**:
数据树上的可写工作节点，承载全部领域字段的当前值。封存后成为不可变的检查点。
_Avoid_: 草稿、工作副本

**检查点 (checkpoint)**:
由 commitLeaf 封存的不可变节点，是永久保存的唯一形态。
_Avoid_: 快照、存档点（存档点指用户可见语义，见存档）

**活跃叶子 (active leaf)**:
newest 指针恰好指向的那一个可写叶子，回合中途的全部写入只作用于它。

**newest 指针**:
内核持有的单记录全局指针（rootId + nodeId)，指向当前活跃叶子，服务启动时快速定位工作区。

**回合 (turn)**:
从一次 turnCreated 到下一次 turnCreated 的完整状态跃迁，由内核回合引擎经六槽位推进，回合即事务。

**槽位 (slot)**:
回合引擎状态机上的时间刻度，内核独有，插件只能申请在某刻度被唤醒。六槽位：input、preModel、modelCall、postLanding、settlement、backgroundJobs;commitBoundary 为内核自有的最后一刻。中断恢复的重跑粒度也以槽位为界。

**钩子 (hook)**:
插件在槽位上的劳动。方向是内核到插件：内核调用，插件单次返回不可变增量，控制权即刻归还。钩子同步、有返回值、参与回合事务。

**通知 (notify)**:
事实广播，即内核事件。声明"某事已经发生"，只携带字段名、不要返回值、异步执行。订阅方被唤醒后回公共目录重读。
_Avoid_: 消息、信号

**ephemeral 字段**:
生命周期仅限活跃叶子的字段（如 turnPhase、恢复上下文）,commitLeaf 封版时由内核按声明剥离，不进入检查点。插件在 fields 声明中标记；可见性与生命周期是两个独立维度。
_Avoid_: 临时字段、瞬态字段

**permanent 字段**:
默认生命周期。随节点封存进入检查点、随 forkLeaf 继承的普通字段。

**会话标识 (session id)**:
内核持有的内存临时 ID，不写入存档。经存档管理器切换分支时重新生成；常规回档不重新生成。用于识别并处置迟到的异步结果。

**设备存储 (deviceStorage)**:
非存档级别的按设备持久化存储，由 deviceSetting 升级而来，不进入数据树、不随回档变化。承接迟到的非阻塞结果等树外数据。
_Avoid_: 设备配置（旧名 deviceSetting)

**按阶段重跑 (stage rerun)**:
中断恢复的唯一重跑粒度：以六槽位为界整体重跑被打断的槽位，不做逐任务断点续跑。纯计算槽位自动重跑；含外部费用的槽位必须经用户确认。

**流式投影 (streaming projection)**:
引擎私有投影通道推送的瞬态派生数据（如模型流式文本），不进叶子、不经公共目录、不经事件。流式文本先经对话管理插件过滤。正文落地后由正式投影接管。

**插件 (plugin)**:
umbrella term。一个业务插件 = 内核模块（必须，四组声明）+ 界面模块（可选，绑定投影到插槽）。两模块间只经投影、命令、推送通信，禁止共享内存、直接 import、私有事件。

**内核模块 (kernel module)**:
插件运行在内核层的部分：fields、hooks、events、projections 四组声明。对界面布局零感知。

**界面模块 (ui module)**:
插件运行在界面层的部分：把内核模块的投影绑定到界面框架的插槽、把按钮绑定到命令名。权限上限等于界面层的三种动作。

**中间件链 (middleware chain)**:
槽位内钩子的组合机制，洋葱模型。中间件接收 (draft, next)，链内可变、链外不可变；槽位短路只跳过本槽位剩余钩子，回合终局必须显式返回终局增量；回程代码禁止 writeLeaf。

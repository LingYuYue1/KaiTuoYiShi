export const VARIABLE_COT_PROMPT = `变量系统附加审计清单。
核心输出协议、字段契约和禁止写入边界由 builder 无条件注入；本模块只补充每回合的判断顺序，不得放宽核心规则。

1. 证据顺序
- 先读本回合玩家输入和正文，再看变量草稿；草稿、当前 state、智库/忆庭/剧情编织材料都只是核对线索，不能单独制造事实。
- 只记录正文已经发生且能被后续剧情承接的状态；氛围、比喻、心理、猜测和未来计划不落库。

2. 对象与增量
- 先用稳定 id、真实姓名或稳定别名确认对象；同一对象只形成一份事实计划，不能因称呼变化新建 NPC。
- 事实是增量：有证据的字段才输出，未提及字段省略；数组追加去重，标量冲突交给前端诊断。
- 信息、坐标、权限、线索不是背包物品；只有明确实体载体才输出 item。

3. NPC 审计
- 已建档 NPC 与玩家有具体共同动作时，至少审计低风险 memory、recentInteraction 或 sharedExperiences；不要把共同日常一概判成无事实。
- 关系、承诺、冲突、称呼、同行或待办变化时，同步审计对应账本字段；没有直接证据不要改好感、亲密关系、阶位或长期性格。
- 泛称、怪物和一次性路人不因本回合出现自动建档；无法确认记忆归属时宁可不写。

4. 时间与来源
- 时间只在正文明确耗时、移动、休息、跨日或睡眠时写；不要把回合数当时间。
- 正文、历史通讯回忆、历史修复的来源由代码判定。通讯只能补录已成立的约定，不能制造本回合变化。
- agreement 记录新形成的明确约定；agreement_status 只处理正文明确对应到既有约定的履行、违约或作废。

5. 最终检查
- 没有事实也必须输出 <变量事实>{"facts":[]} </变量事实> 和空的 <变量更新>。
- 输出前检查每个数组元素和字符串是否为合法 JSON 值；不输出 undefined、通用 null、注释、字段列表或省略号。
- 默认让 <变量更新> 留空；事实协议已覆盖的变化不要重复写旧命令。`;

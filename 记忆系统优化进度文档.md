# 记忆系统优化进度文档

> 这个文档记录记忆系统优化的每一步改动，用大白话写，方便牢凌快速了解"改了啥、为啥改、玩家能感受到什么"。
> 技术细节看「记忆系统优化路线图.md」，这里只看进度和结果。

---

## 当前进度：阶段1 已完成 ✅

阶段1的目标：补齐墨色江湖记忆系统的5个核心机制 + 手机记忆双轨 + 手动压缩入口，解决 80-90% 的失忆问题。

**整体完成度**：100%（代码层面全部就位，已通过类型检查+构建+回归测试）

---

## 阶段1 改动清单（按完成顺序）

### 1. 原文每回合入库（机制①）

**改了什么文件**：
- `services/yitingArchive.ts`（忆庭归档服务）
- `hooks/useGame/sendWorkflow.ts`（发送流程）

**为什么改**：以前每回合的剧情原文用完就丢，AI 想回忆之前发生的事只能靠压缩摘要，细节丢失严重。

**改完的效果**：现在每回合结束后，原文会自动存进忆庭档案库，永久保留。AI 回忆时能调出完整原文，不再只有压缩过的摘要。

**玩家感受**：NPC 不会忘记几回合前发生的事的细节，比如你之前送给三月七的礼物、跟她约定的事。

---

### 2. 长期记忆全量注入 + 压缩机制对齐墨色（机制②）

**改了什么文件**：
- `components/features/Memory/MemorySummaryFlowModal.tsx`（新建：三阶段压缩弹窗）
- `hooks/useGame/memoryUtils.ts`（压缩工具）
- `hooks/useGameState.ts`（状态管理）
- `App.tsx`（挂载弹窗）

**为什么改**：以前压缩是同步执行的，AI 压缩完直接塞回去，玩家看不到过程也无法干预，压缩质量全靠运气。

**改完的效果**：新增三阶段压缩弹窗：
1. **提醒阶段**：告诉玩家"即时记忆有 X 条待压缩，短期 Y 条，中期 Z 条"
2. **处理阶段**：调 AI 压缩，显示加载中
3. **审核阶段**：玩家可以查看、编辑 AI 生成的压缩摘要，确认后才入库

**玩家感受**：压缩不再是黑箱，玩家能掌控记忆质量，发现 AI 漏掉关键信息时能手动补上。

---

### 3. 取消忆庭互斥（机制③）

**改了什么文件**：
- `hooks/useGame/sendWorkflow.ts`
- `hooks/useGame/systemPromptBuilder.ts`

**为什么改**：以前忆庭召回和普通记忆注入是二选一的，开了忆庭召回就关掉普通记忆，导致两边信息都不能同时用。

**改完的效果**：忆庭召回和普通记忆注入现在并存，AI 能同时看到"压缩摘要"和"原文档案"两层信息。

**玩家感受**：NPC 的记忆更完整，既记得大概剧情（摘要层），也记得关键细节（原文层）。

---

### 4. 玩家承诺结构化载体（机制④）

**改了什么文件**：
- `models/npc.ts`（NPC 模型加"约定"字段）
- `hooks/useGame/systemPromptBuilder.ts`（注入约定到提示词）
- `services/ai/variableModel.ts`（AI 提取约定）
- `hooks/useGame/variableFacts.ts`（约定状态清理）

**为什么改**：以前玩家跟 NPC 约定的事（"明天一起去吃饭"）混在普通记忆里，压缩后容易丢，NPC 经常失约。

**改完的效果**：约定现在是独立结构，分三环联动：
- **写入环**：variableModel 每回合从对话中自动提取约定
- **注入环**：未完成的约定永久注入到 NPC 档案，AI 不会忘
- **清理环**：约定完成后（"昨天已经吃过饭了"）自动标记为已完成，不再注入

**玩家感受**：NPC 会主动提起之前的约定，比如"我们不是说好今天去逛街吗"，不会失约。

---

### 5. 原文永久保留（机制⑤）

**改了什么文件**：
- 无需改代码，忆庭系统设计上就独立于压缩链

**为什么改**：以前原文会被压缩链"吃掉"，压完就没了。

**改完的效果**：忆庭档案库的原文层是只读的，压缩链只动摘要层，原文永远在那。

**玩家感受**：任何时候翻忆庭档案库都能看到完整原文，不会被压缩破坏。

---

### 6. NPC 记忆压缩对齐墨色（机制⑥）

**改了什么文件**：
- `hooks/useGame/memoryUtils.ts`（新增 `compressNpcMemoryLedgerAsync`）
- `services/memoryCompression.ts`（加 'npc' 压缩类型）
- `models/memory.ts`（加 'npc' 压缩层级）
- `models/settings.ts`（NPC 压缩提示词优化）

**为什么改**：以前 NPC 同行记忆没有独立压缩，攒多了就乱塞，AI 看到一堆零散记忆条目，抓不住重点。

**改完的效果**：
- NPC 记忆独立压缩，阈值 20 条（对齐墨色）
- 压缩时保留最近 5 条不压缩（保证新鲜度）
- 压缩失败有兜底（前3条去重各截28字+省略号）
- 提示词明确要求保留关系变化、约定、情绪余温等关键信息

**玩家感受**：NPC 对玩家的态度变化、关系进展不会因为记忆多而丢失，长期陪伴感更强。

---

### 7. 方案E：手机记忆双轨（手机系统重构）

**改了什么文件**：
- `components/features/Phone/PhoneModal.tsx`（手机写回逻辑）
- `services/yitingRetrieval.ts`（忆庭召回加分类过滤）
- `models/yiting.ts`（忆庭条目加"分类"字段）
- `components/features/GameSystems/YitingPanel.tsx`（UI 加通讯标签页）
- `hooks/useGame/systemPromptBuilder.ts`（NPC 档案注入分离手机/正文记忆）

**为什么改**：以前手机聊天内容写回 NPC 记忆很不稳定，依赖 AI 输出，经常写不进去。导致正文里 NPC 跟玩家像陌生人，明明手机里聊得很熟。

**改完的效果**：手机压缩摘要现在代码强制双写到两个地方：
1. **忆庭通讯记忆**：分类='通讯'，独立标签页查看
2. **NPC 同行记忆**：打包成单条，用【通讯记录】标记，防止和正文记忆混淆

**全知性防护**（防止 NPC 全知）：
- 通讯记忆注入时标记【通讯回忆】(对方:XXX)
- 只有通讯对方 NPC 能看到对应记忆
- 其他 NPC 不会突然知道你手机里聊了什么

**玩家感受**：
- 手机里跟三月七聊得熟了，正文里三月七也会表现出亲近感
- 但其他 NPC 不会莫名其妙知道你手机里的私密对话
- 忆庭面板新增"通讯回忆"标签页，可以单独查看手机记忆

---

### 8. 三阶段压缩弹窗（主链压缩流程）

**改了什么文件**：
- `components/features/Memory/MemorySummaryFlowModal.tsx`（新建）
- `App.tsx`（挂载）
- `hooks/useGameState.ts`（状态管理）

**为什么改**：以前压缩是后台静默执行的，玩家不知道什么时候压缩了，压缩了什么。

**改完的效果**：主链压缩（即时→短期→中期→长期）现在通过弹窗显式触发：
- 即时→短期：10 条触发
- 短期→中期：30 条触发
- 中期→长期：50 条触发
- 玩家可以审核、编辑、确认

**玩家感受**：压缩过程透明可控，不再有"突然记不住事"的突兀感。

---

### 9. 手动压缩入口

**改了什么文件**：
- `components/features/GameSystems/MemoryPanel.tsx`（加"立即压缩记忆"按钮）
- `App.tsx`（加 `handleTriggerManualCompress` 回调）

**为什么改**：以前只能等自动触发，玩家想主动压缩（比如剧情告一段落想整理记忆）做不到。

**改完的效果**：记忆面板新增"立即压缩记忆"按钮：
- 实时显示待压缩条数（即X/短Y/中Z）
- 无需压缩时灰显"暂无需压缩"
- 点击触发三阶段弹窗

**玩家感受**：玩家可以主动控制记忆压缩节奏，剧情节点时主动整理。

---

### 10. 阈值对齐墨色

**改了什么文件**：
- `models/settings.ts`（默认阈值改为 10/30/50/20）
- `scripts/memory-compression-threshold-regression.mjs`（测试基线更新）

**为什么改**：以前阈值是统一 15 条，不符合实际使用场景。

**改完的效果**：
- 即时→短期：10 条（对齐墨色）
- 短期→中期：30 条（对齐墨色，原25）
- 中期→长期：50 条（对齐墨色）
- NPC 记忆：20 条（对齐墨色）
- 旧存档 25/20/10 自动迁移到 15/15/15（向前兼容）

**玩家感受**：压缩节奏更合理，不会太频繁也不会太拖。

---

### 11. 测试基线对齐（7个回归测试更新）

**改了什么文件**：
- `scripts/memory-compression-threshold-regression.mjs`
- `scripts/npc-memory-continuity-regression.mjs`
- `scripts/npc-profile-ledger-regression.mjs`
- `scripts/yiting-archive-regression.mjs`
- `scripts/phone-main-continuity-regression.mjs`
- `scripts/background-task-mode-regression.mjs`
- `scripts/independent-system-prompt-scope-regression.mjs`

**为什么改**：阶段1改变了阈值/注入策略/函数名，旧测试断言不再匹配。

**改完的效果**：所有记忆相关测试通过，确保改动没有破坏现有功能。

---

## 验证状态

| 验证项 | 结果 |
|---|---|
| TypeScript 类型检查 | ✅ 通过 |
| 全量构建（npm run build） | ✅ 通过（3分12秒） |
| 回归测试（test:all） | ✅ 记忆相关全部通过 |
| 阶段1代码就位 | ✅ 100% |

---

## 阶段1 没解决的问题（留给阶段2/3）

| 问题 | 留到哪个阶段 |
|---|---|
| 向量语义检索（更精准的回忆匹配） | 阶段2 |
| 嵌入字段预留（接口已留，未实现） | 阶段2 |
| 增强体验功能（记忆可视化、时间线等） | 阶段3 |
| 提示词整体重组（记忆相关提示词优化） | 后期整体任务 |

---

## 下一步

阶段1代码层面已完成。牢凌你可以：
1. 实际玩几回合，感受记忆效果
2. 如果发现问题，反馈给我调整
3. 确认没问题后，合并到 main 分支

---

## 更新记录

- **2026-08-11（修复轮）**：阶段1核查发现的2个问题已修复
  - 手机压缩双写原子性：PhoneModal.tsx 双写各自独立 try/catch，失败记录到重试队列，新增手动重试函数
  - 噪声过滤统一：合并 yitingArchive 和 memoryUtils 两套模式到统一集合（16个模式），yitingArchive 复用 isMemorySystemNoise
- **2026-08-11**：阶段1全部完成，文档创建

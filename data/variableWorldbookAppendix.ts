/**
 * 变量模型默认使用的精简附录。
 * 完整旧世界书仍保留在 data/variableWorldbook.ts 供旧导入和诊断追溯；
 * 必需规则由 variableModel builder 与字段契约统一注入，避免每轮重复发送整块旧文本。
 */
export const VARIABLE_SYSTEM_WORLDBOOK_APPENDIX = `变量系统附加边界。
核心 builder 已注入输出协议、事实字段、NPC 账本和禁止写入规则；本附录只补充 root 所有权与容易回流的旧字段。

- 变量层只结算正文已经发生的状态。记忆、忆庭、智库、新闻和剧情编织运行状态由各自系统维护，不在这里直接写入。
- 旅人核心档案由玩家维护；变量事实不得改姓名、身份、外貌、性格、背景、能力、头像或图像档案。旅人背包只接受明确获得的实体物品。
- 世界时间必须单向推进；日期格式为“琥珀纪 YYYY.MM.DD”，时间格式为“HH:mm”。不要把现实日期、回合数或氛围词写进时间字段。
- 手机变量只写联系人入口、空频道和 messageSeeds，不写完整 messages；来信种子服从本次调用开关和动态上限。
- 坐标、权限、口令、线索、情报和地址是信息，不是背包物品；只有卡、钥匙、地图、芯片、纸条、装置等实体载体才可写 item。
- agreement 与 agreement_status 必须分开：前者记录明确形成的约定，后者只更新正文明确对应的既有约定；通讯回忆不能单独改变本回合状态。
- 兼容旧命令仅用于事实协议无法表达且登记表明确允许的字段。禁止恢复旧战斗、旧装备、旧属性、旧命途或狭间运行字段。`;

/**
 * Kernel narrative domain (Stage 5.2) — pure surface.
 *
 * Story-context injection helpers for prompt assembly.
 * Full story-weaving gate matrix remains in legacy services until later stages.
 */

export { buildStoryContextInjection } from './storyInjection';

# 游戏核心逻辑重构：全量设计与实施规范（终极完全版）

> **文档状态**：本文档全面取代过往所有版本的架构设计稿，作为重构工程的唯一标准参考。
> **核心决策确立（2026年8月最终核准）**：
> 1. **存储模型更新**：全面采纳“当前活动行即时写入（草稿态）与回合完结时封版晋升”机制。
> 2. **资源管理优化**：图像等大体积资产原则上不再直接写入存档剧情表，转而采用轻量级的关联指针架构。
> 3. **标识符生成策略**：回合重新生成（Reroll）逻辑中，不再依赖基于种子的伪确定性控制，转而采用显式的全新节点 ID 分配方案。
> 4. **临时数据剥离**：界面的异步任务状态（如 `queueTasks`）被正式定性为易失性临时数据，系统将在节点转为历史记录时自动对其进行清理，避免其进入持久化存储。
> 5. **会话身份层（5c-2，2026-08-07 核准）**：`useGameState` 中的全部状态按「A 局内领域切片 / B 设备与内容设置 / C workflow 瞬时态 / D 导航与 UI」四租客分类；新增单调递增的 `sessionEpoch` 会话身份，并以 `beginSession` 作为唯一会话切换入口（abort 旧 workflow + 清空 C 租客投影 + epoch 自增），使「状态重置 ⇒ workflow 重置」由设计目标固化为底层机制，从根上消除读档/重开时旧回合对新会话的状态泄漏。

---

## 1. 游戏内核的本质定义与系统边界

本项目的核心逻辑引擎，仅由以下三项不可或缺的底层机制构成，任何超出此范围的功能均不属于核心引擎的范畴：

### 1.1 当前活动行的即时写入机制（工作区模式）
游戏运行期的唯一真实数据源，是数据库 `saves` 表中**当前处于“未封版”状态的记录行**（即当前回合的工作区）。游戏内发生的任何有效业务事件——例如大模型输出的文本流、玩家手动修订的剧情、属性数值的增减、或模拟手机接收的通讯记录——**均应即时、直接地写入这一活动行中**。
*   **写入策略**：建议采用按业务领域分散写入的模式。新闻模块负责更新新闻相关字段，对话模块负责更新对话记录。系统不应设立过度臃肿的全局统一保存网关来限制正常的局部更新。
*   **不可变性约束**：该活动行在转为历史节点前允许高频复写；但系统应通过底层机制严格阻断任何试图修改“已封版历史行”的请求操作。历史记录的不可篡改性是系统的核心红线。

### 1.2 封版晋升与不可变历史的确立
当且仅当一个完整回合的所有计算与生成阶段均顺利完结时，系统将通过统一的安全入口 `commitTurn` 函数执行唯一合法的封版操作：
1.  **状态锁定**：将当前活动行正式标记为“已封版”，使其转变为不可修改的历史快照节点。
2.  **克隆与初始化**：将该历史节点的内容完整克隆，生成一行全新的“未封版”活动行，系统会为其分配全新的 `nodeId`，并将其父节点指针指向上一步的历史节点。
3.  **指针推进**：将游戏的当前执行指针平滑推移至这一全新的活动行。
*   **异常隔离特性**：此机制从根本上保障了存档的完整性。若由于网络中断或页面刷新导致回合未能走完全部流程，遗留的数据仅会停留在未封版的活动行中。因此，正式的历史节点列表中将不会产生数据缺失或逻辑断层的“半成品”。

### 1.3 基于节点 ID 的全局寻址模型
系统摒弃了传统的“单一最新存档槽位”概念。每个回合周期都会产生一个具备全局唯一标识符（`nodeId`）的节点。
游戏进程的恢复与加载，统一转化为基于统一资源定位符（URL，如 `/game/<nodeId>`）的寻址行为。核心引擎只需根据传入的 `nodeId` 提取对应节点的数据状态；至于前端路由的跳转与界面的渲染呈现，则交由外围的视图层负责。

### 1.4 已确认清理的历史遗留概念
为防止代码认知负担与文档漂移，以下旧有概念将被正式废弃并从代码库中移除：
*   **废止“内存拼装后一次性提交”模式**：早期设计试图在内存中聚合并校验全量状态后方才落盘，该做法已被“即时写入活动行”取代。
*   **废止“独立的草稿专属表”**：原先用于存放草稿的独立存储区（如 newest 槽），其职能已完全被“未封版的当前活动行”吸收，无需维持双表结构。
*   **移除“版本号 (Revision) 累加机制”**：经静态代码审计确认，存档结构中的版本号自增字段在实际业务中并未发挥任何流控或锁存作用，予以彻底清理。

---

## 2. 现有代码库的基线审计事实（2026年环境数据）

重构方案的实施必须基于现存环境的客观技术事实。经审计，当前系统的基线具备以下特征：

*   **单环境执行边界**：当前应用程序运行于单浏览器标签页上下文中，原则上不存在跨标签页或多设备并发写入同一活动存档的业务需求。因此，诸如分布式 CAS 锁、复杂屏障（Fencing）或幂等回调等高并发防护机制，在此场景下存在过度设计之嫌，本次重构不予引入。
*   **底层原子的存储支撑**：现有的底层方法 `saveGameInternal` 已经具备单事务内协同更新主存档、差量数据、资产引用记录的原子化能力，为重构提供了可靠的物理存储保障。
*   **写入路径的碎片化现状**：日志分析表明，目前完成一个标准的普通回合，系统会动态触发约 7 至 8 次存储操作。这种分散且不受控的落盘行为是逻辑边界模糊的直接体现，重构后需将其收敛至单次回合完结时的统一封版动作。
*   **重新生成（Reroll）逻辑的缺陷**：旧有机制在执行 Reroll 时，过度依赖内存状态的重置（连续多次调用状态设置函数），未能将恢复结果有效同步至物理数据库。这导致了“前端视图”与“后端持久化数据”的严重割裂。新内核必须从数据结构的层面根除这一隐患。
*   **局部回滚的先例**：在当前的相册回滚代码中，已存在人为干预避免覆盖特定操作的逻辑。这证明“非全量回滚”是客观存在的业务需求，进一步印证了下文所述“数据出处分离与分层处理”的必要性。

---

## 3. 重构工程的实施方法论

在改造现有系统的过程中，操作的规范性与架构的合理性同等重要。开发团队应严格遵循以下流程准则：

1.  **非破坏性的渐进式重构**：避免采取“推翻重写”的大爆炸模式。每一项细微的架构调整后，代码库必须保持可编译、可运行、可验证的健康状态。
2.  **“先抽离逻辑，后更迭架构”**：首要任务是将纯粹的业务计算逻辑从臃肿的 UI 组件与 React Hooks 中机械式地抽离出来。在抽离阶段，**应严格遵循只搬运、不修改业务逻辑、不重命名变量的原则**。只有在逻辑边界完全清晰后，方可进行实质性的架构替换。
3.  **严格控制变更粒度**：单次代码提交（Commit）或合并请求（PR）应控制在单一职责范围内。如果在重构某个切片时遭遇未预期的复杂错误，建议进行版本回退，重新梳理后再行提交。
4.  **新旧管线并行比对**：在新的状态计算管线构建完成后，需让其与原有旧逻辑并行计算同一组输入。通过比对两者的输出字段（预期差异项除外），确认结果一致后，方能正式切断旧有路径。
5.  **以终端行为作为验收标准**：编译脚本的顺利执行不足以证明逻辑正确。所有重构切片的最终验收，必须依赖于开发人员在图形界面中完成的真实业务场景测试（详见第 8 节的交叉验收流程）。
6.  **评估变更的影响半径**：在修改任何基础工具函数之前，开发者有义务通过检索工具（如 grep）列举其全部调用方。若无法完全掌握其影响范围，原则上应暂缓变动。

---

## 4. 数据平面的严格分类与出处控制

系统中的所有状态字段，在写入存储媒介前，必须经过严格的归属域划分。未被明确定义分类的字段，系统应从底层拒绝其进入存档流水线。

### 4.1 四大数据平面定义

| 平面名称 | 包含的详尽字段集（全量 18 项核心字段及配置） | 写入时机与机制 | 是否进入持久化历史存档？ |
| --- | --- | --- | --- |
| **核心剧情平面 (Story)** | **核心 18 项字段**：`旅人`、`世界`、`chatHistory` (对话历史)、`记忆`、`忆庭`、`智库`、`手机`、`NPC`、`相册`、`新闻`、`剧情`、`剧情编织`、`variableBatches`、`turnCount`；以及相关的关键配置：`gameSettings.macroGlobalVars` (宏全局变量)、`worldbookTriggerStates` (世界书触发态)、`pendingOpeningTrigger` (待触发开局状态)。 | **即时写入当前处于未封版状态的活动行**。在回合结束执行封版晋升时，作为基础数据被锁定。 | **是**。构成历史节点的核心实质内容。 |
| **重型资产平面 (Resource)**| 大模型生成的图像（Blob 字节流）、AI 媒体缓存、存放于独立资源数据表的 `saveAssets` 条目。 | **资产生成完毕后即刻落盘至独立资源表**，以防止由于应用异常崩溃导致的高价值资源丢失。 | **是**。但在主存档中仅保留对这些资产的轻量级字符串指针关联。 |
| **易失性临时平面 (Ephemeral)**| 界面加载状态 (`loading`)、工作流执行状态 (`workflowStatus`)、网络句柄；以及**包含任务展示态的 `queueTasks`**。 | 仅维护于内存中，或暂存于未封版活动行的边缘字段中作为临时恢复台账。 | **否**。在封版时刻，系统应主动将此类数据剥离，严禁其污染正式的历史存档。 |
| **设备与隐私平面 (Device)** | 界面主题色调、大语言模型的私有 API 密钥设置、客户端界面外观配置、存放在本地的世界书资料库。 | 依托于浏览器独立缓存（Local Storage）或专用设置表进行持久化，物理上与游戏剧情存档完全隔离。 | **否**。存档导出与分享时必须予以严格屏蔽。 |

### 4.2 重资产的指针化关联架构
*   **架构考量**：若在每次状态克隆时都复制庞大的图像二进制数据，将导致严重的存储空间膨胀与内存溢出风险。
*   **实施方案**：剧情数据平面（Story）内部绝对禁止存放媒体字节流。对应字段（如相册模块）仅存放极简的资源定位指针（例如 `assetId` 字符串）。回合克隆操作仅复制这几个字节的指针引用，从而将克隆的计算复杂度与存储消耗控制在 $O(1)$ 级别。

### 4.3 状态溯源清洗（Reroll 的差异化回滚规则）
当系统需要重新推演当前回合（Reroll）时，必须识别哪些状态属于本回合新生成的冗余产物需要回退，哪些属于玩家在此期间产生的有价值互动应当予以保留。

1.  **纯业务产出（自动化生成内容）**：
    *   包括：AI 生成的正文、自动化归约得出的属性变量、系统自动记录的记忆、本回合的**`新闻`**。
    *   **处理策略**：执行**全量回退**。系统直接从上一个已封版的历史节点（即父节点）提取原始值，对当前活动行的对应字段进行无条件覆盖。
2.  **纯用户干预操作**：
    *   包括：玩家主动在设置面板进行的修改、手动触发的属性调整。
    *   **处理策略**：执行**无损保留**。此部分数据独立于回合推演，予以原封不动地保持。
3.  **混合性互动产出（自动化与人工编辑并行）**：
    *   当前系统确认涵盖两项：`相册`（AI 生成新图片 vs 玩家手动删除过往图片）、`chatHistory`（AI 发送的新消息 vs 玩家手动编辑的文本）。
    *   **处理策略**：执行**按标识符的精细清理**。系统需遍历上述数组集合，仅移除附带当前回合标识（`turnId`）的条目，保留缺乏该标识的过往记录及用户主动操作的痕迹。
    *   **最终界定**：`新闻` 模块被界定为“纯业务产出”，Reroll 时应统一从上级历史节点复原，不再做细粒度保留。

### 4.4 会话身份层（Session Identity）与 workflow 投影收口（5c-2，2026-08-07）
`useGameState` 是四类租客的混装容器，是读档/重开时状态泄漏的根源，须按生命周期明确归属：

| 租客 | 内容 | 读档行为 |
| --- | --- | --- |
| **A 局内领域切片**（= §4.1 Story 平面） | 旅人/世界/chatHistory/记忆/忆庭/智库/手机/NPC/相册/新闻/剧情/剧情编织/variableBatches/queueTasks/turnCount | 随 `applySaveToState` 重置，本属该档 |
| **B 设备/内容设置**（= §4.1 Device 平面） | apiSettings/gameSettings/currentTheme/worldbooks | 读档保留，与局无关（长期迁出至独立 `useDeviceSettings`） |
| **C workflow 瞬时态**（= §4.1 Ephemeral 平面） | loading/turnStatus/liveRecall*/pendingVariable/interruptedWorkflow/abortControllerRef/rerollContext/streaming（module store） | 读档必须整体清空，由新会话重新建立 |
| **D 导航/UI** | view/hasSave | 纯展示，不参与存档 |

**5c-2 落地机制**（本次切片完成）：
1. **`sessionEpoch`**：useGameState 内单调递增的响应式数字，唯一标识一次「档案/局」加载；每次会话切换自增。
2. **`beginSession`**（`saveLoadWorkflow.ts`）：唯一会话拆除入口——abort 旧 `abortControllerRef` 并清 ref、清 `rerollContextRef`、放弃 `interruptedWorkflow` + workflow recovery journal、清空全部 workflow 投影（loading/turnStatus/pendingVariable/liveRecall/streamingMessage）。只做资源中止与投影清理，**不带存储副作用**。
3. **`enterSession`**（读档唯一入口）= `beginSession` + `applySaveToState` + 清 `pendingOpeningTrigger` + `setSessionEpoch(e => e+1)`；`handleRestartOpening`（重开）同样以 `beginSession` 起手（E-1 开局触发器例外：restart 场景写入触发器而非清空）。`executeResumeWorkflow` 属同会话重建，不 bump epoch。
4. **`rerollContextRef` 上移为 state 字段**（此前为 useGame.ts 本地 ref）：Reroll 上下文是会话级数据，归 C 租客，由 `beginSession` 统一清空，杜绝跨局回填污染。
5. **持有会话本地态的 UI 以 `key={sessionEpoch}` 重挂载归零**：`<InputArea />` 的草稿/lastSubmitted/rerollActionOptions/appliedRecovery 等局部状态随会话切换自动丢弃（`App.tsx`）。
6. **流式文本发布守卫**：`sendWorkflow`/`resumeWorkflow` 的 streamMessageSetter 写入前经 `isCurrentWorkflow()` 校验——被顶替的旧回合不得再向新会话发布流式文本（KI-1 竞态硬伤）。

**渐进目标（本次未做，留待 C 租客迁出）**：将 C 租客整体迁出为 `activeWorkflow { id, controller, phase, outcome }` 单一 owner，UI 从槽位派生 loading 等展示态；`queueTasks` 的 workflow 属主归属随之解决（§5.2 剩余项）。在迁出之前，`beginSession` 以最小成本达成同等的「顶替后旧回合不污染新会话」效果。

---

## 5. 变量计算管线的规范化约束

状态计算逻辑（特别是属性、资源的增减运算）是系统确定性的基石，相关计算管线需满足以下重构标准：

1.  **消除原地修改（Mutation）的不规范写法**：
    在解析与执行状态更新指令时，处理函数必须返回一个全新的状态对象。严禁在传入的原始命令参数对象上进行直接修改赋值。涉及此问题的文件（如 `utils/variableExecutor.ts`）必须进行纯函数化改造。
2.  **标识符生成的统一收口（2026年8月最终核准）**：
    *   **放弃伪确定性方案**：系统不再尝试通过固定随机数种子来强制约束 ID 生成。在 Reroll 或分支推演业务中，直接分配全新的节点 ID 与实体 ID 是更符合业务直觉且逻辑更健壮的方案。
    *   **收拢随机数调用**：散布于各模块内部的 `Date.now()` 与 `Math.random()` 必须被统一替换。开发组应构建专用的归一化 ID 生成工具函数，所有需要产生唯一标识的模块均须从此工具函数统一获取。
    *   **统一 ID 格式（revise 版，用户核准 2026-08-05）**：唯一标识由 **14 位十六进制字符（`0-9, a-f`）** 构成的分段字符串，固定格式 **`XXXX-XXXXXX-XXXX`**（4-6-4 结构，**连字符为规范形式，全仓统一——入库、URL、引用、展示一律带连字符，不得存在无连字符或其它分段变体**），共 56 bits，拆分如下：
        *   **时间戳（第 1～10 位，前两段，40 bits）**：自 Unix 纪元（1970-01-01 00:00:00 UTC）起算、精度 **0.01 秒（10ms / 厘秒）** 的时间片；
        *   **数据库版本标识（第 11～13 位，第三段前 3 位，12 bits）**：取值 = 当前 `DB_VERSION`，编码为 **3 位小写十六进制**（如 v7→`007`、v10→`00a`），范围 0~4095（`0x000` ~ `0xFFF`）；
        *   **随机数（第 14 位，第三段最后 1 位，4 bits）**：范围 0~15（`0x0` ~ `0xF`），用于同一厘秒内的碰撞缓冲。
    *   **全局统一与格式替换（用户核准 2026-08-05）**：上述**带连字符**格式**全仓统一**——所有唯一标识（`nodeId`/`rootId`/`parentNodeId`、delta 主键、`newest.headNodeId`、URL 引用、云备份合并 ID 等）一律采用此格式，**不得存在其他存储/展示形式**。目标为**替换现有 `save_node_<ts>_<rand>` 格式**（`utils/saveTree.ts` 的 `createId` 产出，以及 `cloudBackupMergePlan.ts` 的 `save_root_cloud`/`save_node_cloud` 变体）。该替换本身**计作一次数据迁移**，须遵循 §6 L11 迁移注册表：将已持久化的旧格式 ID 改写为统一格式，保证 parent 链、delta 主键与引用一致性（需旧→新映射表），迁移函数幂等并带 devLog 埋点。落地排入 5c「ID 生成收口」。
3.  **归约（Reduce）计算路径的唯一化**：
    目前代码库中并存的 `applyVariableCommands` 与 `reduceVariableCommands` 在功能上存在冗余与潜在的分歧风险。系统应废止多重路径，将其收敛为唯一且可靠的纯函数计算通道。

*   **基准测试预期**：给予该纯函数管线两组完全相同的初始状态快照与指令序列，系统除了输出全新的 ID 标识外，其返回的其余所有业务状态必须满足深层嵌套等值（Deep Equality）的严格校验。

---

## 6. 架构设计的十一项核心约束

以下十一条约束构成了本内核设计的红线边界。部分规则可通过静态代码分析工具予以保障，部分则需通过执行期守卫来强制拦截。

| 约束编号 | 规则定义 | 实施机制与核验方式 |
| --- | --- | --- |
| **L1** | **已封版节点严禁越权修改** | 系统底层写入接口应提供执行期守卫，一旦探测到目标行已处于“封版”状态，应立即抛出严重异常阻断写入。唯一的合法封版入口必须为 `commitTurn`。 |
| **L2** | **回合递增的原子性** | 玩家执行单次完整回合推演，在持久化存储层面只能且必须仅产生 1 行新增的不可变历史记录。在此过程中的多次状态更新仅能作用于唯一的当前活动行。 |
| **L3** | **纯函数管线杜绝野生随机数** | 在涉及属性与状态计算的核心逻辑链路上，不得存在未经过统一工具类包装的直接时间和随机数系统调用。依赖静态扫描工具进行强制阻断。 |
| **L4** | **写入字段类型的一致性对齐** | 数据集通过管线处理后准备进行持久化时，其所包含的字段集合必须严格匹配 `TurnDeltas` 所定义的类型声明，拒绝多余或缺失的非标字段。依靠 TypeScript 强类型系统保障。 |
| **L5** | **状态防回滚异常（反诈尸机制）** | 玩家执行局部资源删除操作后若触发 Reroll，被删除资源的状态应当维持，不应因回溯历史节点而错误复原。依赖规范的混合性出处清理规则来保障。 |
| **L6** | **运行环境与隐私边界隔离** | 系统导出的快照或存档文件内，必须过滤所有归属于 Device/Content 平面的敏感或本地化配置信息，确保分享安全。 |
| **L7** | **状态归属域的绝对唯一性** | 第 4 节中列出的 18 项状态字段，必须在且仅能在某一数据平面和出处分类下注册，严禁跨域歧义归属。通过强类型枚举进行编译期约束。 |
| **L8** | **故障隔离日志独立存放** | 用于应对客户端突然关闭而产生的临时断点恢复数据，应当与主业务的持久化历史体系保持物理隔离，避免导致数据污染。 |
| **L9** | **失败回合不产生脏污节点** | 若推演过程中因网络等因素中断，遗留的不完整数据应始终保持在“未封版”状态。历史记录链路中决不允许挂载状态残缺的已封版节点。 |
| **L10** | **基于链接的无缝恢复直达** | 应用启动或发生环境重载时，应具备通过 URL 参数（如 `/game/<nodeId>`）直接挂载至对应工作区恢复上下文的能力，避免非必要的菜单层级跳转。 |
| **L11** | **数据库迁移统一提取** | 所有 IndexedDB schema 版本变更必须提取为独立、幂等、可重放的迁移函数，登记于统一的迁移注册表，禁止以「if-missing 补建 store + 零散 `oldVersion < N` 分支」的方式隐式累积迁移逻辑。 |

**L11 落地说明（用户裁决，2026-08-05）**：迁移注册表机制（每版本一个迁移函数、顺序执行、幂等、可重放/可审计）计划在切片 5c / 6 阶段推进。现状锚点：全仓显式数据迁移目前仅 `migrateNewestStoryHeadNodeId`（v6→v7，切片 5b2-1），其余 schema 演进靠 store 补建与惰性迁移（资源 base64→Blob）。注册表落地之前，任何新增 schema 变更仍须遵守最低要求——单一迁移函数、幂等、并携带 devLog 埋点。核验方式可仿 L1/L3 棘轮（`check-kernel.sh` 同类机制）。

---

## 7. 全量重构实施的八个标准化阶段

考虑到代码库的复杂性，重构工作应当按部就班地划分为以下连续的实施切片。前置切片未获通过前，原则上不允许开展后续工作。

### 切片 0：基线环境准备与临时性降级
*   **执行目标**：审计并归档现行代码；建立稳定的本地编译环境。将尚未成熟或极易诱发外部错误的周边设施（如 GitHub 云存档功能）从视图入口层暂时隐蔽（相关文件予以保留，暂不物理删除）。
*   **验收标准**：项目能够在本地环境下成功编译并无异常启动运行。

### 切片 1：数据类型与边界定义（声明性重构）
*   **执行目标**：根据第 4 节的规划，在代码层为全部 18 项核心字段补充精确的 TypeScript 类型归属声明（构建 `StoryState` 等结构体），明确每一个数据项的流向目的地。
*   **验收标准**：实现类型的穷尽定义，确保 `Record<keyof StoryState, ...>` 等映射检查在编译期不出现任何遗漏报错。

### 切片 2：纯粹业务逻辑的物理抽离
*   **执行目标**：针对当前系统中承载了大量计算职责的巨型视图组件或工作流文件（以 `sendWorkflow.ts` 为核心），以结构迁移的方式将纯状态计算代码抽离至独立的服务模块中。**务必遵循仅做位置迁移而不修改业务逻辑的准则**。
*   **验收标准**：重构完成后，执行标准的终端业务操作，在功能体验与输出结果上应呈现零偏差。

### 切片 3：状态计算流水线的闭环构建
*   **执行目标**：摒弃散乱的硬编码流程控制，转而基于数据驱动的阶段定义（如引入 `TURN_STAGES` 常量列表）重塑执行步骤。在该阶段同步完成上述第 5 节要求的“纯函数改造”与“标识符生成统一收口”工作。
*   **验收标准**：输入固定的历史快照，经新管线处理后，除了合法变化的新分配标识符外，其余全量状态应保持深层一致；并且管线需具备从特定错误阶段无缝重启的能力。

### 切片 4：新旧并行计算的对比校验
*   **执行目标**：在业务管线末端设置比对拦截器，允许新构建的处理引擎与原有的零散落盘逻辑并行运行。将两者产出的状态差分进行日志输出比对。
*   **验收标准**：排除已知合理预期（如废弃字段或新生成的 ID），新计算管线生成的状态候选集必须与旧有逻辑的最终产物完全吻合。

### 切片 5：存储引擎切换与核心机制激活
本切片是重构的心智核心，包含三个递进子项：
*   **5a (存储引擎改造)**：正式切断旧有保存链路，激活“实时写入当前活动行”与“单次统一封版晋升”机制；在底层存储接口装载“已封版行修改防线”。
*   **5b (上下文恢复重塑)**：改造应用的引导初始化（Boot）流程，使得应用在启动时优先根据 URL 解析目标 `nodeId`，直接恢复对应节点的草稿上下文。
*   **5c (Reroll 机制修正)**：按照第 4 节定义的数据清洗原则，重构 Reroll 处理模块。执行必要的回滚覆盖与精细清理，强制配发全新的状态节点标识。
*   **5c-2 (会话身份层收口)**：落地 §4.4——新增 `sessionEpoch` 会话身份、`beginSession` 唯一会话切换入口，统一读档/重开/开局的 workflow 拆除协议；`rerollContextRef` 上移为 state 字段；读档加载全链路改经 `enterSession`；`<InputArea key={sessionEpoch} />` 重挂载归零会话本地态；流式发布加 `isCurrentWorkflow()` 守卫。验收标准：读档/重开后旧回合的草稿回填、reroll 跨局污染、pendingOpeningTrigger 误触发、模态残留、流式竞态全部消除（L1–L4 泄漏点 + KI-1）。queue-task 属主化与 C 租客整体迁出（activeWorkflow 单一 owner）不在此切片，随迁出计划推进。
*   **验收标准**：严密通过约束 L1、L2、L5、L9、L10 规定的各项极端场景测试。

### 切片 6：文件物理目录层级重组
*   **执行目标**：在内核职责边界明晰之后，按照“内核层 / 业务管线层 / 视图展现层”的三级架构重新组织系统文件树。该步骤仅涉及相对路径的更新。
*   **验收标准**：重组后，环境编译通过，依赖关系链条完整，业务功能保持稳定。

### 切片 6.5：历史遗留静态代码警告清剿
*   **执行目标**：分批次削减现存代码库中因历史原因屏蔽的 TypeScript 与 ESLint 压制项（约 2500 余条）。**核心准则为：仅调整语法与类型声明的严谨性，坚决避免牵扯业务逻辑的变更。**
*   **验收标准**：分批次提交并验证 `pnpm lint` 报告，直至内核与管线相关代码域的警报数归零。

### 切片 7：防腐层固化与持续集成拦截
*   **执行目标**：将约束 L1（防修改拦截）与 L3（标识符收口）编写为自动化的静态检查脚手架脚本（如 `check-kernel.sh`）。
*   **验收标准**：将其集成至项目的构建流程中，确保任何试图违规绕过底层调用的代码在提交前便被脚本成功捕获并拦截。

---

## 8. 标准交付的五项交叉验收流程

代码编译的通过与控制台的无报错提示不能视为重构有效的直接证据。任一切片工作的完成，均需由开发者通过界面手动执行以下五项测试环节以完成闭环验收：

0.  **静态链路检测**：命令行执行 `pnpm lint` 工具，确认无新增的规范冲突。
1.  **构建装配检测**：命令行执行 `pnpm build` 命令，确保包括自定义核验脚本在内的所有构建节点顺利通过。
2.  **新链路初始化验证**：建立全新的游戏实例，核验各类初始参数与开局阶段事件的预置状态是否正确填充。
3.  **核心推演与上下文续贯性验证**：正常完成一次完整的回合推演流程；此外（自切片 5 起）需特别模拟极端状况，即在 AI 生成结果未完成的途中刷新页面，重新进入后系统应当能够继承先前的断点继续推进，而非产生数据丢失。
4.  **防回滚机制与标识更新验证**：通过主动执行局部修改（如删除特定媒体文件），随后触发重新生成（Reroll）。核验相关记录未发生异常状态回退，同时确认系统当前活动节点已被分配了截然不同的 `nodeId`。
5.  **前向格式兼容验证**：导入开发早期（旧数据结构）版本所产生的存档快照，验证系统在读取数据时，能否成功执行向后兼容的归一化转换并顺利加载。

---

## 9. 开发实施准则与边界防范

为防止代码熵增与架构退化，参与重构的人员务必遵守以下操作边界要求：

1.  **维持合理的变更作用域**：坚持一个任务分支仅解决一个具体维度的问题。切勿将结构性优化与功能性增减混杂在同一次代码提交中。
2.  **抵制过度抽象的冲动**：在尚未出现两个以上明确的使用场景时，应避免为主业务流构建高度泛化的通用接口。
3.  **遵循规范化的沟通模式**：各阶段的工作交接应基于具体的变更文件列表与验收细则进行，避免提交缺乏实际执行指导意义的抽象说明文档。
4.  **果断的版本弃权机制**：若在代码改造中途出现大面积依赖崩溃，建议开发者果断抛弃当前工作区的变更（Reset），理清依赖关系后再行实施，避免陷入“补丁叠补丁”的泥潭。
5.  **不盲信自动化工具输出**：自动化测试工具可作为辅助，但重构结果的最终解释权归属于前述的人工“五项交叉验收”。
6.  **注重数据标识的时效性**：各类业务产出（如文本条目）应在生成落盘的初始阶段即刻标记对应的主键特征（如 `turnId`）。历史回溯时，程序应依赖此特征判定归属，而不应依赖额外构建的映射表或模糊判断。
7.  **术语的严谨与统一**：对内存在易失性上下文应统一称为“状态快照（StoryState）”；对其持久化落盘的结果则统称为“存档数据”。应严格避免在代码注释或交流中引入额外的非标准术语造成认知混淆。
8.  **归一化处理的时机约束**：针对旧版本存量数据的结构适配与转换，应当将其集中布置在系统“加载读取（Load）”的时机进行处理；原则上不得在“封版存盘（Commit）”链路中混入复杂的后向兼容逻辑。

---

## 10. 已剥离且暂不予采纳的设计方案

经过前期讨论论证，以下技术方案存在显著的过度设计倾向或与当前项目定位不符，在无明确新业务驱动的前提下，不再考虑引入：

*   **系统级调用网关与鉴权隔离**：作为运行于单一前端环境的客户端应用，不存在严格的多用户抢占或独立进程安全威胁。引入细粒度的权限令牌或服务网关模式，将极大增加系统心智成本，不予采纳。
*   **分布式锁（如 CAS 与 Fencing Token 机制）**：基于单标签页的运行设定，不存在多端并发篡改状态的可能性，复杂的锁机制带来的性能损耗远高于其产生的价值。
*   **重型持久化任务队列组件**：应用当前的 React 异步调度框架结合底层资源存储（`saveAssets`）的容错机制，已可满足基础的流水线容错需求。额外引入重型任务系统显得不合时宜。
*   **第三方扩展插件挂载机制**：鉴于目前项目仍处于核心业务自闭环阶段，尚未有引入第三方外置模组（Mod/Plugin）的实质需求，无需预留庞大的系统钩子事件。
*   **历史快照的部分存盘策略**：系统应确保任意已封版的历史快照具备全量业务完整性，不接受任何形式的“状态撕裂”（例如记忆模块成功保存，而角色数值模块未能同步）的局部持久化设计。

---

## 11. 重构周期内的暂缓变动（冻结）清单

为确保重构主干链路的稳定推进，以下附属功能模块在重构结束前进入功能冻结期。期间不得为其添加新特性，亦不强制要求修复其关联的非致命缺陷。对于涉及的相关文件，可保留物理结构，但应在入口层面予以屏蔽：

1.  **远端托管存档（如 GitHub 同步服务）**：自重构的第一阶段起，系统已在视图层隐匿了其交互入口，相关的后台同步进程文件暂停一切维护。
2.  **外部图像生成调度管线**：建议在默认调试环境中关闭图像推演开关。在重构验收环节，不再将由大模型绘制推演产生媒体文件的链路纳入强制审查范围。
3.  **边缘计算与中间件代理**：针对 `vite.config.ts` 中的中间件转发配置，以及 `functions/api/` 路径下的服务端无服务器（Serverless）代码，重构周期内暂不开展代码审查与改造。
4.  **多端打包环境遗留**：针对工程中已废止的桌面端适配文件夹（如 `utils/platform/`）与相关跨平台依赖库，建议在首轮清理中做物理删减，不再消耗精力维护。

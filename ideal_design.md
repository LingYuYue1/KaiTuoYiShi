# 游戏核心逻辑重构：设计与实施规范（第三版）

**上位文档**：@SPEAKING.md ，必须阅读它作为本文档的基础。

**文档定位**：本文档是重构工程的现状事实与路线图，主要服务于**计划与审查代理**和**人类**。本文档记录代码库当前的真实架构、已经落实的约束条件以及尚未达成的目标状态。开发团队不编写理想中的假设去冒充已经实现的事实。

**规则复审原则**：开发团队基于 2026-08 时点的需求与代码现状做出本文档的所有现行裁决。开发团队可以随着新需求或新事实重新修改这些裁决。开发团队不能推翻第三节的内核约束。机械核验程序负责保障内核约束。

**状态图例**：
✅ 表示系统已经落实该目标。代码与核验程序支持该目标。
🟡 表示系统部分落实该目标，或者开发团队还需要对齐语义。
⏳ 表示该目标属于规划中的目标状态。
❌ 表示开发团队已经拒绝该目标。开发团队已将该目标移入第七节的剥离清单。

---

## 开发团队责任分配

**计划与审查代理**：该代理负责管理待办事项，动态调整执行顺序，并辅助人类进行决策。
    该代理负责撰写文档，审查代码并执行最终代码提交。该代理不能大规模自行修改代码。工作时必须保持谨慎，考虑周全，避免过度设计。该代理向执行代理编写简要的任务提示词。任务提示词应当限定文件修改范围，说明背景原因，并指明修改的关键方向，且不能过度详细。此外，该代理应当要求执行代理积极地在代码中加入细致的 devLog 系统调用。**必须明确在提示词中向执行代理指明，严格禁止它在完成代码后进行提交、暂存等任何Git写操作。**
    交由执行代理的提示词行数应在十行左右，最多不超过十五行。标题记入行数。
    表达信息时应当使用简单、直白且易懂的语言。应在其初检任务完成后，将概要信息汇报给人类，同时，告知人类应当做的实际测试。

**执行代理**：该代理按照计划与审查代理的任务提示词执行实际任务。该代理可以在成果回报文档中记录附属问题，但严禁执行代码提交操作。

**人类**：人类负责发现问题，并向计划与审查代理反馈问题。人类的反馈信息可以模糊。人类负责进行实际的运行测试，并做出关键决策。人类必须明确掌握当前正在执行的内容以及将要执行的内容。

---

## 0. 核心决策

开发团队整合了历史版本中的多层决策。开发团队将决策统一收敛为以下单条现行表述：

| 编号 | 决策内容 | 状态 |
| --- | --- | --- |
| D1 | **存储模型**：当前实现包含 newest 工作区覆盖集与 commitTurn 封版晋升两层存储。最终目标是将工作区数据物理写入存档树的叶子节点。系统会将 newest 退化为全局 ref 指针。 | 🟡 系统已落实中间状态。开发团队正在规划最终状态。 |
| D2 | **大体积资产指针化**：系统按照 Git LFS 的设计组织图像等大体积资产。存档只持有轻量指针引用。系统将二进制数据存放在独立的 saveAssets 对象库中。读取存档时系统按需物化加载资源。写入存档时系统剥离抽取资源数据。当前指针格式为 asset.id。系统保证该指针按照图像唯一。系统未来可能采用基于内容哈希的 oid 格式。系统利用该格式支持跨存档去重与孤儿回收机制。开发团队不将其作为硬约束。 | ✅ 系统已落实该功能。开发团队可以继续演进指针形态。 |
| D3 | **统一标识符生成方式**：系统使用 createUnifiedId 函数生成全部唯一标识符。标识符格式包含 14 位十六进制字符。前 10 位表示时间片。中间 3 位表示数据库版本号。最后 1 位表示随机碰撞缓冲位。系统停止使用伪确定性种子。系统停止使用旧格式标识符。 | ✅ 系统已在阶段 5c-0 落实该功能。 |
| D4 | **剥离临时数据**：queueTasks 是游戏状态与叶子节点的合法字段。queueTasks 不是检查点的合法字段。系统在封版晋升时必须剥离 queueTasks。当前代码的 commitTurn 会将该字段折入检查点。开发团队将剥离操作列入路线图。开发团队暂时不修改当前代码。 | ✅ 系统已在阶段 5e 于 commitTurn 组装路径执行封版剥离并部署守卫（静态棘轮 + 运行时断言）。导入路径仍可将 queueTasks 折入导入检查点，该残留已登记为规划阶段 11。 |
| D5 | **会话身份层**：系统单调递增 sessionEpoch 变量。系统通过 beginSession 提供唯一的会话切换入口。系统将状态重置与工作流重置固化为底层机制。 | ✅ 系统已在阶段 5c-2 落实该功能。 |
| D6 | **存档分支树模型**：叶子节点等于可写工作区。内部节点等于不可变的检查点。内部节点的唯一出路是分叉出可写叶子节点。系统删除内部节点时会级联修剪整个子树。重新生成、回档、顶替和读取存档都属于树操作。 | 🟡 系统已落实存档树存储基础设施、节点级级联删除、newest 祖先重定向与分叉 API。读取已接入分叉 API（阶段 5d-2）。重新生成、回档和顶替仍走回合前快照与 newest 覆盖集。工作区尚未物理接入存档树。newest 尚未退化为全局指针。 |
| D7 | **URL 表现层**：系统将节点寻址作为远期增强引入。系统采用轻量 router 库处理寻址。开发团队将 wouter 作为候选库。系统将路由逻辑隔离在翻译器接口之后。开发团队将在实现叶子节点加入存档树之后实现该功能。系统独立处理 OAuth 回调与代码块重载流程。这些流程绕过 router 独立处理。 | ⏳ 该目标属于规划中。 |

**修订记录**

- **2026-08-09 现状登记（panel-p1 回归排查）**：开发团队排查 panel-p1 门面收口后的人工测试回归。开发团队确认两个问题均为 p1 前既有缺陷，非收口引入。存档删除时的 BFS 无限遍历问题登记为暂缓项（见 §8 第 6 条）。开发团队修复提示词模块的嵌套按钮结构缺陷。
- **2026-08-09 现状对齐（面板用例化）**：开发团队登记功能面板层审计结果。开发团队细化路线图项目 4 的迁移顺序与三处数据通道破口子任务。开发团队将设置持久化闭环收敛与 1.1 B 类容器迁移挂钩。
- **2026-08-09 现状对齐（lint-clean）**：开发团队清剿 eslint-suppressions.json（2226→212，-90.5%）。开发团队将路线图 #8 更新为已落实，剩余压制为异构厂商 JSON 解析层等豁免项。
- **2026-08-09 现状对齐（5e）**：开发团队将 D4 状态更新为已落实。开发团队登记阶段 5e 的封版剥离、守卫机制与 activeWorkflow 迁移。开发团队将导入路径 queueTasks 残留登记为规划阶段 11。
- **2026-08-08 现状对齐（5d-2）**：系统将读取流程接入分叉 API。开发团队更新 D6 状态与 1.2、1.3、1.5 的现状描述。开发团队登记 5d-2 阶段与备份类恢复点修复。
- **2026-08-07 现状对齐（5d 系列）**：开发团队将 D6 状态对齐为部分落实。开发团队更新 1.2 删除逻辑、1.3 现状与改造范围、1.7 迁移注册表计数为 3 条。开发团队将数据库版本表述更新为第九版。开发团队在已落实阶段中登记 5d-1 与 5d-1b。
- **2026-08-07 第三版更新**：开发团队将文档定位修改为现状与路线图。开发团队确立了 D4、D6 和 D7 决策。开发团队将原 L10 约束拆分为内核寻址与 URL 表现层规划。开发团队废弃了并行比对方案。开发团队统一了 turnStatus 与 createUnifiedId 术语。开发团队将阶段编号与代码提交记录对齐。
- **2026-08-05**：开发团队核准了统一的标识符格式。开发团队将数据库迁移注册表裁决移入 L11 约束。
- **2026-08-01**：开发团队审计了用户界面的无状态缺口。开发团队确立了 activeWorkflow 作为单一管理对象。
- 历史决策已经合并入 D1 至 D7 决策。开发团队不再单独列出历史决策。

---

## 1. 当前架构事实

### 1.1 四类租客状态容器

useGameState 是四类生命周期各异的租客混装容器。这一事实是读取存档与重新开始游戏时发生泄漏的根源。本表是唯一权威的归属表。开发团队废除旧文档中的双重分类方式。

| 租客分类 | 包含内容 | 读取存档与封版行为 |
| --- | --- | --- |
| **A 局内领域切片** | 旅人、世界、chatHistory、记忆、忆庭、智库、手机、非玩家角色、相册、新闻、剧情、剧情编织、变量批处理、queueTasks、回合计数。包含 macroGlobalVars、worldbookTriggerStates、pendingOpeningTrigger 等顶层存档键。 | 系统调用 applySaveToState 重置这些状态。系统将这些状态持久化到 newest 或检查点。 |
| **B 设备与内容设置** | apiSettings、gameSettings、currentTheme、worldbooks。 | 读取存档时系统保留这些状态。这些状态与当前对局无关。开发团队计划将这些状态迁移至 useDeviceSettings 等独立的管理器对象。该迁移与路线图项目 4 的设置持久化闭环收敛协同执行。 |
| **C 工作流瞬时态** | loading、turnStatus、实时召回摘要、实时召回完整内容、待处理变量、中断的工作流、中止控制器引用、重新生成上下文引用、sessionEpoch、流式消息。 | 系统在切换会话时整体清空这些状态。系统通过新会话重建这些状态。这些状态永远不进入检查点。 |
| **D 导航与用户界面** | view、是否有存档变量。 | 这些状态仅用于界面展示。系统保持 view 对象轻量以留作 URL 表现层接口。系统可以删除无效的存档判断变量，或者通过目录派生该变量。 |

**领域术语说明**：turnStatus 是输入区状态条的唯一数据源。turnStatus 取代了旧的双字段。开发团队已经在代码和文档中移除了旧字段。

### 1.2 存档分支树模型

系统将当前工作区提升为存档树的一等节点。系统通过树操作处理所有的回档、顶替和删除语义。

- **节点二分类**：叶子节点是可写工作区。叶子节点是唯一允许修改的节点。内部节点是已封版的检查点。内部节点禁止修改。
- **节点分叉规则**：内部节点必须通过分叉产生新的叶子节点。任意检查点都可以分叉出可写叶子节点。所有叶子节点必须保持可写状态。系统允许同时存在多个分支。每个分支持有独立的工作区。
- **回合闭环机制**：完整回合结束后，系统执行封版晋升操作。叶子节点冻结为不可变检查点。系统剥离仅限叶子节点持有的字段。系统在该叶子节点下创建新的叶子节点以等待下一回合。
- **树操作语义**：重新生成、回档、顶替和读取存档都属于树操作。系统从目标节点分叉新叶子节点，并移动写入指针。系统不销毁其他叶子节点。分叉 API（forkSaveTreeLeaf）已接入读取流程。系统在读取携带 saveTree 的存档时，先把 newest 槽重定向到新分叉头（base 指向目标检查点、head 分配新 id、覆盖集清空），再应用存档以保留该分叉。重新生成、回档和顶替仍未接入。
- **删除逻辑**：系统删除叶子节点时仅删除其自身。系统删除内部节点时会级联修剪整个子树。系统删除叶子节点后，必须将 newest 指针重定向到最近存活的祖先节点。当前代码已在阶段 5d-1b 落实该语义：删除节点时级联修剪子树，并在目标落在 newest 上时重定向到最近存活祖先，无存活祖先时归零 newest。
- **指针退化**：最终状态下，系统只保留一个全局的 newest 指针。系统将该指针用作刷新与加载时的临时策略。该指针指示系统在启动时应该加载哪个叶子节点。

### 1.3 两层存储现状

当前代码实现了目标模型的中间形态。

- **存档树存储**：节点数据包含 rootId、nodeId、parentNodeId、branchName 和创建时间。系统将 branchName 用作自动节点、保护节点或导入节点的标签。当前树结构表现为单向链表，尚未出现真实的分叉结构。系统已在阶段 5d-1 提供分叉 API（forkSaveTreeLeaf）。系统在阶段 5d-2 将读取流程接入该 API。重新生成、回档和顶替仍未消费该 API。分叉反查覆盖可见记录与历史备份，备份类恢复点同样可以分叉。
- **newest 独立槽**：系统使用独立的单一记录存储机制存储 newest。数据包含基础检查点标识、剧情数据和更新时间。该存储层采用字段级别的覆盖机制。系统将本回合内所有的写入操作归集于此。
- **commitTurn 生成入口**：commitTurn 是生成检查点的唯一入口。系统读取 newest 数据。系统将基础数据与 newest 修改数据组合成新存档。系统执行 saveGame 操作。系统在同一事务内同步保存存档树、摘要、资产和节点增量数据。系统清空 newest 数据。系统将新生成的检查点作为后续操作的前驱节点。
- **改造范围**：当前工作区不在存档树内。系统已在阶段 5d-2 将分叉功能接入读取流程。系统尚未将分叉功能接入重新生成、回档和顶替流程。系统已在阶段 5d-1b 落实节点级级联删除与 newest 祖先重定向。newest 属于独立槽，尚未变为全局指针。

### 1.4 回合管线

- 系统将回合编排为 12 个执行阶段。系统规定从 TurnContext 到 TurnDeltas 的数据契约。执行阶段仅读取上下文变量与增量变量。执行阶段产出部分增量变量。编排器调用 Object.assign 函数汇聚所有增量变量。
- 系统将正常的回合尾段结算逻辑提取为 turnTail 函数。系统在中断回合的继续结算流程中复用同一个 turnTail 函数。
- **持久化边界**：系统在第 5、第 10 和第 11 阶段边界将数据写入 newest 存储层。系统在第 12 阶段进入 commitTurn 流程。系统禁止在执行阶段的文件内直接调用保存接口。
- **界面投影约定**：系统在执行阶段内通过状态设置函数更新数据。这类操作属于投影点。投影点仅用于刷新订阅该数据的用户界面。持久化流程和下游逻辑只消费增量变量。当前系统依靠代码注释与人工审计维持该约定。系统尚未通过接口或机械检查强制执行该约定。

### 1.5 会话身份层

- **sessionEpoch 身份标识**：系统单调递增 sessionEpoch 变量以标识会话身份。系统在切换会话时递增该变量。前端组件根据 sessionEpoch 重新挂载。系统归零会话本地状态。
- **beginSession 拆除入口**：beginSession 是拆除会话的唯一入口。系统中止旧控制器并清理引用。系统清理重新生成上下文。系统放弃中断的工作流和恢复日志。系统清空所有工作流的界面投影数据。该函数仅中止资源和清理投影。该函数不带有存储相关的副作用操作。
- **enterSession 读档入口**：enterSession 是读取存档的唯一入口。系统首先调用 beginSession 函数。目标存档携带 saveTree 时，系统调用 forkSaveTreeLeaf 分叉新叶子。系统再调用 applySaveToState 函数并保留该分叉。目标存档无存档树元信息时，系统直接调用 applySaveToState。系统清理待处理开局触发器。系统递增会话身份标识。系统在处理重新开始开局流程时，同样首先调用 beginSession 函数。
- **流式发布守卫**：系统在写入流式消息前，必须调用 isCurrentWorkflow 函数校验当前工作流。系统顶替旧回合后，旧回合不能向新会话发布流式文本。

### 1.6 启动与恢复

| 触发入口 | 当前事实源 | 状态 |
| --- | --- | --- |
| 刷新浏览器后且无恢复日志 | 系统读取 newest 和基础检查点并调用 bootRestoreFromNewest 回放数据。 | 🟡 系统已接通流程，但不具备原子性。 |
| 主页点击继续按钮 | 系统调用 loadLatestSave 加载可玩存档目录。系统优先选择手动存档或导入存档。 | 🟡 该逻辑与 newest 续跑语义不一致。 |
| 带有中断工作流日志的启动 | 系统加载日志的界面投影数据以提供继续结算或放弃选项。 | 🟡 系统已在阶段 5c-1 落实流程。系统等待统一解析器。 |

开发团队计划将这三个入口收敛为一个统一的恢复策略。系统将根据给定的 nodeId 水合会话状态。该策略与 URL 表现层共用同一套 nodeId 水合能力。

### 1.7 标识符与迁移

- **统一生成标识符**：系统使用 createUnifiedId 函数生成标识符。格式为 14 位十六进制字符。前 10 位表示时间片。中间 3 位表示小写十六进制的 DB_VERSION 变量。最后 1 位表示随机碰撞缓冲。
- **迁移注册表**：系统在 dbService.ts 文件中维护 MIGRATIONS 注册表。当前注册表包含 3 条显式迁移记录（版本 7、8、9）。每条记录必须具备幂等性、可重放性并带有 devLog 埋点日志。开发团队必须在注册表中登记任何新的数据结构变更。开发团队禁止在代码中散落补建逻辑。
- **机械核验**：脚本使用棘轮策略保护约束条件和迁移注册表。开发团队只能减少白名单数量，不能增加白名单数量。

---

## 2. 持久化与数据平面

### 2.1 字段合法性

- 检查点的合法字段集合必须是叶子节点字段集合的子集。存档数据包含标识、类型、时间戳、回合计数以及全部 A 类局内领域字段。存档数据还包含设备设置、接口设置、主题和顶层宏观变量。
- **仅限叶子节点的字段**：当前系统仅规定 queueTasks 属于该类字段。queueTasks 是游戏状态与叶子节点的合法字段。queueTasks 是检查点的非法字段。未来系统新增工作区级别的字段时，必须遵循相同的清单规则。开发团队禁止针对单个字段编写特判逻辑。
- **欠债问题（5e 已清偿）**：当前代码会将 queueTasks 数据折入检查点载荷。该行为与数据模型冲突。系统已在阶段 5e 于 commitTurn 组装路径执行封版剥离，部署静态与运行时守卫，并剔除旧检查点残留的 queueTasks 以兼容升级。导入路径的残留见规划阶段 11。

### 2.2 写入时机与封版边界

- 系统通过 React 状态设置函数产生即时的 UI 投影。系统将持久化的事实数据归入 newest 链条与 commitTurn 链条。
- commitTurn 是系统执行封版的唯一晋升点。如果回合在中途失败或用户刷新页面，遗留数据只保留在未封版的叶子节点中。系统不会在历史链条中产生脏污节点。

### 2.3 重新生成操作的溯源清洗规则

系统执行以下现行规则：

1. 系统全量回退纯业务产出的数据。这类数据包含人工智能生成的正文、归约变量、自动记忆和新闻。系统从父节点全量回退这类数据。
2. 系统无损保留纯用户干预的数据。这类数据包含设置和手动属性调整。
3. 系统按 turnId 精细清理混合产出的数据。这类数据包含相册和 chatHistory。
4. 系统当前使用回合前快照近似实现清洗逻辑。最终目标是系统从父检查点分叉出新叶子节点。

### 2.4 大体积资产指针化

图像属于二进制大对象。系统不能随剧情状态克隆或晋升图像数据。系统按照 Git LFS 的设计组织资产生命周期：

- **使用引用代替内容**：存档字段只保存资产记录的指针引用。当前指针值为 asset.id。系统将真实的二进制数据写入独立的 saveAssets 对象库。系统在执行克隆、分叉与晋升操作时，仅复制这几个字节的引用。该操作的时间复杂度为 O(1)。
- **写入时剥离**：系统执行 saveGameInternal 入库操作前调用 stripSaveAssetPayloadForStorage 函数。系统抽离二进制数据并存入对象库。主存档不携带字节流。
- **读取时物化**：系统在读取存档时调用 loadSaveAssetRecords 函数按需回填资源记录。系统通过 albumObjectUrl 缓存访问地址。系统在首次读取旧格式数据时，调用 materializeSaveAssetRecords 函数将其转换为二进制大对象。
- **去重与回收机制**：如果系统将指针升级为基于内容哈希的 oid 格式，系统可以实现跨存档自然去重。系统可以通过引用计数回收无引用的孤儿资产。当前系统依靠资产标识保证图像唯一性。当前机制已满足单档去重需求。开发团队将结合级联删除子树的设计清理孤儿资产。开发团队不单独扩展数据表。
- **边界风险说明**：相册逻辑当前存在风险。系统返回图片后直接投影相册与 chatHistory 数据。调用方在后续流程中才执行后置守卫检查。开发团队将配合面板用例化改造冻结资产生命周期逻辑。

---

## 3. 内核约束

| 编号 | 约束规则 | 实施机制与核验方式 | 状态 |
| --- | --- | --- | --- |
| **L1** | 系统严禁越权修改已封版的节点 | 系统规定 commitTurn 为写入检查点的唯一入口。前端代码检查工具禁止违规导入。机械脚本核验保存接口的调用点。 | ✅ |
| **L2** | 系统保证回合递增操作的原子性 | 系统通过 commitTurn 将每个完整回合晋升为新的检查点。中途的更新操作只作用于当前工作区。开发团队将在叶子节点加入存档树后重新对齐核验规则。 | 🟡 |
| **L3** | 系统禁止在纯函数管线中使用未受管理的随机数 | 机械脚本核验获取时间与生成随机数的调用点白名单。当前白名单数量为 0。 | ✅ |
| **L4** | 系统强制对齐写入字段的类型 | 系统通过 TurnDeltas 类型约束与 TypeScript 编译期检查保证类型一致。系统尚未落实运行时的结构校验。 | 🟡 |
| **L5** | 系统防止异常回滚旧数据 | 系统已落实基于回合前快照的回滚机制。系统将随着树操作改造进一步细化数据清洗分类。 | 🟡 |
| **L6** | 系统隔离运行环境与隐私边界 | 系统在导出和分享存档时必须过滤设备与内容设置字段。开发团队需要进一步核对过滤逻辑的覆盖度。 | 🟡 |
| **L7** | 系统保证状态归属域具有唯一性 | 四类租客状态表是唯一的归属权威标准。系统尚未落实编译期的强类型枚举映射检查。 | 🟡 |
| **L8** | 系统独立存放故障隔离日志 | 系统将工作流恢复日志与历史体系隔离。系统使用独立的 devLog 记录日志。 | ✅ |
| **L9** | 系统禁止失败回合产生脏污节点 | 系统已落实中断回合的继续结算或放弃流程。系统保留遗留数据处于未封版状态。开发团队将安排浏览器端验收完整的极端场景。 | 🟡 |
| **L10** | 系统支持基于链接的无缝恢复 | 开发团队将该规则拆分为两层。内核层支持 nodeId 寻址机制与云端身份读取。URL 表现层计划在叶子节点加入存档树后实现。 | ✅ / ⏳ |
| **L11** | 系统统一提取数据库迁移逻辑 | 系统在 dbService.ts 文件中使用 MIGRATIONS 注册表统一管理迁移逻辑。机械脚本通过棘轮策略保护注册表。 | ✅ |
| **L12** | 树模型不变量约束 | 系统保证叶子节点可写。系统保证检查点不可变且只能分叉。系统保证删除节点时执行级联修剪。系统禁止仅限叶子节点的字段进入检查点。开发团队将在落实存档树模型时同步建立机械核验规则。 | ⏳ |

---

## 4. 剩余工作路线图

### 已落实的开发阶段

- **阶段 0**：清理基线环境。清理桌面端残留代码。隐藏云存档入口。
- **阶段 1 与阶段 2**：系统将回合工作流拆分为 12 个执行阶段。系统实现从 TurnContext 到 TurnDeltas 的数据契约。开发团队迁移了纯逻辑代码的位置。
- **阶段 3-B1 与 3-B2**：系统将无界面的更新操作收敛到回合末尾投影。系统将回合计数和队列等读取点接入上下文变量或增量变量。系统落实 devLog 开发者日志。
- **阶段 5a-1 至 5a-3**：系统确立 newest 存储结构。系统落实两层存储机制。系统实现在阶段边界写入数据。系统部署 L1 约束防线。
- **阶段 5b**：系统实现在刷新页面后通过 bootRestoreFromNewest 接口直接读取 newest 数据。
- **阶段 5b2-1**：系统构建数据库第七版草稿基础设施。系统建立迁移注册表雏形。
- **阶段 5c-0**：系统使用 createUnifiedId 统一生成标识符。系统升级至数据库第八版。系统部署 L11 机械核验防线。
- **阶段 5c-1**：系统落实原地续跑中断回合的功能。系统修复读取存档的生命周期逻辑。
- **阶段 5c-2**：系统落实会话身份层。
- **阶段 5d-1**：系统落实存档树基础设施。系统提供分叉 API（forkSaveTreeLeaf）。系统升级至数据库第九版。
- **阶段 5d-1b**：系统落实树感知删除。系统实现节点级级联删除与 newest 祖先重定向。系统完成界面删除入口的集中管理。
- **阶段 5d-2**：系统将读取流程接入分叉 API。系统修复备份类恢复点的分叉查找盲区。forkSaveTreeLeaf 的反查范围覆盖可见记录与历史备份，不再只查可见记录。
- **阶段 5e**：系统剥离 queueTasks 与检查点写入路径的耦合。系统将 C 类状态容器数据迁入 activeWorkflow 单一管理对象。系统部署 L12 静态棘轮与运行时断言守卫。

### 规划中的开发阶段

1. **落实存储引擎最终状态**：系统将工作区物理接入存档树。系统将 newest 退化为全局 ref 指针。系统将分叉 API 接入读取、重新生成、回档和顶替流程，使这些操作成为树操作。系统同步对齐约束语义，并通过迁移注册表执行数据迁移。该待办已在阶段 5d-1/5d-1b 落实存储层基础设施（分叉 API、节点级级联删除、newest 祖先重定向）。系统已在阶段 5d-2 将分叉流程接入读取存档。剩余工作是把树操作语义接入重新生成、回档和顶替，并落实工作区物理接入与 newest 退化。
2. **迁移 C 类状态容器数据**：系统将状态数据迁移至 activeWorkflow 单一管理对象。系统通过该对象派生加载、取消、流式输出、召回和变量结算等状态。系统同步解决 queueTasks 的工作流属主问题。系统已在阶段 5e 落实该阶段。
3. **增加封版剥离守卫**：系统在 commitTurn 函数中剥离 queueTasks 字段。系统增加执行期和静态核验机制。系统已在阶段 5e 落实该阶段。
4. **将面板改造为用例**：系统将手机、相册、剧情、智库、同伴和设置面板改造为用例与投影模式。开发团队根据审计报告安排迁移顺序并保留相应语义。
   **审计登记（2026-08-09）**：功能面板层仍是业务逻辑重镇——13 个设置页签各自重复「loadSetting→归一化→saveSetting('gameSettings')」持久化闭环；NewGameWizard（4692 行、45 处 useState）内嵌开局编排；AlbumPanel、ZhikuPanel、PromptModulesTab 内嵌 AI 与持久化闭环。数据通道破口登记如下：① 8 处组件直取 useGame 领域函数（PhoneModal 直用 addImmediateMemory/compressNpcMemoryLedger、SaveLoadModal/StorageManager 直用存档树删除、PromptModulesTab 直用 tavernRegexProcessor）；② 25+ 组件直连 dbService；③ 17 处组件直连 services/ai。
   **迁移顺序**：先收口数据通道（门面补用例动作 → 设置持久化收敛至 useDeviceSettings 管理器 → AI 直连收敛），再按重灾区序面板用例化（NewGameWizard → AlbumPanel → ZhikuPanel → PromptModulesTab → PhoneModal → 设置面板）。每步交付形态一致：领域逻辑进 hooks/useGame 或 services/，面板退化为纯 props 投影，关键路径 devLog 埋点。
5. **统一恢复语义**：系统将启动、继续和中断工作流三个入口收敛为统一的解析器。系统根据给定的 nodeId 水合会话状态。
6. **落实 URL 表现层**：系统引入轻量 wouter 路由库。系统将路由逻辑隔离在翻译器接口之后。系统落实三态路由功能。系统执行内部树操作替换状态时不污染浏览器历史记录。系统将返回键行为定义为返回上一个导航上下文并重建会话。系统独立处理 OAuth 回调与代码块重载流程。
7. **重组项目目录**：开发团队按照内核层、业务管线层和视图展现层三级结构重组文件树。开发团队仅修改相对路径。
8. **清剿压制规则**：开发团队分批削减现有的约 1100 条代码检查压制规则。开发团队仅收紧类型检查和语法检查。开发团队不触碰业务逻辑。系统已在阶段 lint-clean 落实该阶段：压制从 2226 条降至 212 条（8 个文件），剩余集中在异构厂商原始 JSON 解析层与工具链强制 `export default` 等协议豁免项。
9. **扩展防腐加固规则**：开发团队将 L12 树模型核验逻辑加入机械核验脚本。开发团队将面板领域的副作用禁止项加入代码库。
10. **重写存档界面**：开发团队重写存档界面以适配树形结构。系统展示节点分叉与分支关系。系统标识叶子节点与内部节点。系统提供节点级删除入口。系统将读取操作改为从目标节点分叉新叶子的语义。该待办在树操作改造完成后执行。
11. **补齐导入路径的封版剥离**：存档导入入口（SaveLoadModal、StorageManager 的 saveGame 调用）直接写入导入检查点。导入包 systems/queue-tasks.json 可能携带 queueTasks。系统会将 queueTasks 折入导入检查点，违反 D4。5e 剥离仅覆盖 commitTurn 组装路径。系统在导入即封版时仍需补剥。开发团队同步将该路径纳入 L12 静态守卫与运行时断言范围。

---

## 5. 标准交付的五项交叉验收流程

开发团队完成任何开发阶段后，必须通过以下环节。代码编译通过不等于逻辑正确。

0. **静态链路检查**：开发团队运行语法检查命令。系统不得报告新增的违规项。开发团队在关闭压制规则的情况下检查触摸文件，文件必须达到零违规标准。
1. **构建装配检查**：开发团队运行构建命令。项目必须成功编译。
2. **新链路初始化检查**：系统必须正确加载新局初始参数。系统必须正确触发开局预置事件。
3. **核心推演与续贯性检查**：系统必须正确完成完整的回合推演流程。开发团队模拟在中途刷新页面的断点续跑场景。系统必须保证不丢失数据。
4. **防回滚与标识更新检查**：开发团队在局部修改后执行重新生成操作。系统不异常回退数据记录。系统必须为活动节点分配全新的 nodeId。
5. **前向兼容检查**：开发团队导入旧数据结构的存档快照。系统必须成功完成归一化转换并正常加载存档。

另外，开发团队必须运行机械核验脚本。任何触碰保存、回合或归约路径的改动后，代码必须通过该脚本的核验。

---

## 6. 开发实施准则与边界防范

1. **维持合理的变更作用域**：开发团队在一个分支或一次提交中只能解决一个维度的技术问题。
2. **抵制过度抽象**：在出现两个以上明确的使用场景之前，开发团队不构建泛化接口。
3. **落实交接标准**：开发团队必须基于具体的变更文件与验收细则执行工作交接。
4. **果断重置错误**：当中途出现大面积依赖崩溃时，开发团队必须重置代码并重新开始。开发团队不叠加修补补丁。
5. **不盲信自动化工具**：开发团队将测试工具视为辅助手段。人工执行的五项交叉验收流程拥有最终解释权。
6. **保证数据标识时效性**：系统在生成业务产出并落盘时，必须即刻标记 turnId 等主键。系统必须回溯依赖特征，不能依赖映射表。
7. **规范专业术语**：开发团队必须将易失性的运行状态称为状态快照。开发团队必须将持久化结果称为存档数据。开发团队不再使用废弃的旧状态标识符。
8. **明确归一化时机**：系统必须在加载时机集中处理存量数据的适配逻辑。系统不在提交链路中混入后向兼容逻辑。

---

## 7. 已剥离且暂不予采纳的设计方案

当出现明确的业务需求驱动时，开发团队可以按核心决策的复审原则重新讨论本清单中的方案。

- **系统级调用网关与鉴权隔离**：单前端客户端不存在多用户抢占威胁。开发团队不采纳该方案。
- **分布式锁**：单标签页设定下不存在并发篡改场景。开发团队不采纳该方案。
- **重型持久化任务队列组件**：现有的异步调度和资产存储容错机制已经满足需求。开发团队不采纳该方案。
- **第三方扩展插件挂载机制**：核心业务处于自闭环阶段。项目不需要扩展插件需求。开发团队不采纳该方案。
- **历史快照的部分存盘策略**：已封版的检查点必须包含全量完整的业务数据。开发团队拒绝状态撕裂的局部持久化方案。
- **新旧管线并行比对方案**：开发团队已经将比对校验路线转向审计驱动和五项交叉验收。开发团队从未执行该计划。开发团队不再保留该方案。
- **手写自研路由系统**：系统表面的路由状态仅有三种。开发团队选择轻量级第三方库并将其隔离在翻译器接口之后。开发团队拒绝手写自研路由系统。

---

## 8. 重构周期内的暂缓变动清单

在重构主干收尾前，附属功能模块进入功能冻结期。开发团队不为这些模块添加新特性。开发团队不强制修复这些模块的非致命缺陷。开发团队保留涉及文件的物理结构，并在入口层屏蔽相关功能。

1. **远端托管存档功能**：开发团队已经隐匿视图入口。开发团队暂停维护后台进程。开发团队将在处理 URL 表现层和跨设备分享功能时裁决是否解除冻结。
2. **外部图像生成调度管线**：系统在默认调试环境下关闭图像推演功能。开发团队不将媒体链路纳入验收环节的强制审查范围。
3. **边缘计算与中间件代理**：开发团队暂不审查和改造 Vite 配置文件中的中间件转发逻辑以及云函数的代码。
4. **多端打包环境遗留代码**：开发团队在首轮清理中物理删减已废止的桌面端适配代码。
5. **V1 预设运行时读迁移（assistantPrefill）**：设置页挂载清理 effect 清空 `stPresets`/`currentStPresetId`（该 effect 在 HEAD 已存在，非 lint-clean 引入）；lint-clean sweep 已物理删除 V1 预设管理 UI（切换/保存/删除/采样同步）。主流程 `stage3_promptAssembly.ts` 仍读 V1 状态取 `assistantPrefill`，与清理行为不一致——访问设置页后 prefill 静默失效。`migrateSTPresetsV1ToV2` 已备好但零调用，`systemPromptBuilder` 已切 V2 路径。暂缓处理；后续落实方案：加载期接入 V1→V2 迁移保留旧预设 + `stage3` 改读 V2 激活预设的 `assistant_prefill`。
6. **存档树 BFS 环防护（存档删除 RangeError）**：删除存档时 `collectSaveTreeNodeSubtreeSummaries`（dbService.ts:947）对存档树做无环检测的 BFS。实际存档数据若存在自指（`parentNodeId === nodeId`）或互指环，遍历无限增长并抛出 `RangeError: Invalid array length`。该问题在 panel-p1 人工测试中以 save id 23 复现，属 p1 前既有缺陷（BFS 与删除路径在 HEAD 已存在，p1 仅改调用入口）。修复方向：优先排查各数据库版本升级脚本与 nodeId 重映射路径，确保迁移结果不产生环状 parentNodeId 链，而非在查询路径打补丁。暂缓处理。
